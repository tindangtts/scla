---
phase: 32-integration-fixes-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/web/src/app/admin/ (renamed from artifacts/web/src/app/(admin)/)
  - artifacts/web/src/app/admin/upgrade-requests/actions.ts
  - artifacts/web/src/app/(resident)/layout.tsx
  - artifacts/web/src/app/(resident)/page.tsx
autonomous: true
requirements: [ADM-01, ADM-02, ADM-03, ADM-04, ADM-05, ADM-06, ADM-07, ADM-08, ADM-09, ADM-10]

must_haves:
  truths:
    - "Admin sidebar links (/admin/dashboard, /admin/tickets, etc.) resolve to real pages, not 404s"
    - "Admin mutations (approve upgrade, update ticket, etc.) redirect/revalidate to correct /admin/* pages"
    - "Bottom nav taps perform client-side navigation (no full page reload)"
    - "Dashboard quick action cards are visible in dark mode"
    - "pnpm format:check passes with zero errors"
  artifacts:
    - path: "artifacts/web/src/app/admin/layout.tsx"
      provides: "Admin layout at /admin/* URL path (not route group)"
    - path: "artifacts/web/src/app/admin/upgrade-requests/actions.ts"
      provides: "Correct revalidatePath(/admin/upgrade-requests)"
    - path: "artifacts/web/src/app/(resident)/layout.tsx"
      provides: "Bottom nav with Next.js Link components"
    - path: "artifacts/web/src/app/(resident)/page.tsx"
      provides: "Quick action cards with dark mode classes"
  key_links:
    - from: "Admin sidebar nav links"
      to: "artifacts/web/src/app/admin/*/page.tsx"
      via: "Next.js file-system routing (/admin/* prefix)"
      pattern: "href.*\\/admin\\/"
    - from: "Bottom nav Link components"
      to: "Next.js client-side router"
      via: "next/link import"
      pattern: "<Link href="
---

<objective>
Fix admin routing, bottom nav client-side navigation, dark mode gaps, and code formatting.

Purpose: Close all gaps from v3.0 milestone audit so admin portal is fully functional and resident UX is polished.
Output: Working admin routing, client-side bottom nav, dark mode cards, clean formatting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-integration-fixes-polish/32-CONTEXT.md
@.planning/v3.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix admin routing and resident polish</name>
  <files>
    artifacts/web/src/app/admin/ (renamed from (admin))
    artifacts/web/src/app/admin/upgrade-requests/actions.ts
    artifacts/web/src/app/(resident)/layout.tsx
    artifacts/web/src/app/(resident)/page.tsx
  </files>
  <read_first>
    - artifacts/web/src/app/(admin)/layout.tsx (confirm it exists before rename)
    - artifacts/web/src/app/(admin)/upgrade-requests/actions.ts (lines 94, 126 have wrong revalidatePath)
    - artifacts/web/src/app/(resident)/layout.tsx (lines 45-59 have <a> tags in bottom nav)
    - artifacts/web/src/app/(resident)/page.tsx (lines 167-186 have quick action cards missing dark classes)
  </read_first>
  <action>
    STEP 1 - Rename admin route group to segment (CRITICAL):
    Run `git mv artifacts/web/src/app/\(admin\) artifacts/web/src/app/admin` to rename the directory.
    This single operation fixes:
    - All 9 sidebar nav links (already point to /admin/*) now resolve correctly
    - All redirect() calls (already use /admin/*) now land on real pages
    - All revalidatePath() calls (already use /admin/*) now target correct paths
    - Middleware pathname.startsWith("/admin") now protects all admin pages

    After rename, verify the directory exists: `ls artifacts/web/src/app/admin/layout.tsx`

    STEP 2 - Fix revalidatePath in upgrade-requests/actions.ts:
    In `artifacts/web/src/app/admin/upgrade-requests/actions.ts` (note: path changed after rename),
    change line 94: `revalidatePath("/upgrade-requests")` to `revalidatePath("/admin/upgrade-requests")`
    change line 126: `revalidatePath("/upgrade-requests")` to `revalidatePath("/admin/upgrade-requests")`

    STEP 3 - Replace bottom nav <a> with <Link> in resident layout:
    In `artifacts/web/src/app/(resident)/layout.tsx`:
    - The file already imports `Link` from `next/link` (it does NOT — check and add `import Link from "next/link"` if missing)
    - Actually, check existing imports. The file uses `requireAuth`, `logout`, `PushPrompt`, etc. but does NOT import Link.
    - Add `import Link from "next/link";` to the imports
    - Replace all 5 bottom nav `<a href="...">` with `<Link href="...">` (lines 45-59)
    - Replace corresponding `</a>` with `</Link>`
    - The 5 links are: "/" (home), "/bills", "/star-assist", "/bookings", "/more"

    STEP 4 - Add dark mode classes to quick action cards:
    In `artifacts/web/src/app/(resident)/page.tsx`, lines 168-186:
    Change the 3 quick action card Link elements' className from:
      `"flex flex-col items-center gap-1 p-3 bg-white border rounded-lg text-center hover:bg-muted/50"`
    to:
      `"flex flex-col items-center gap-1 p-3 bg-white dark:bg-gray-800 border dark:border-gray-700 rounded-lg text-center hover:bg-muted/50"`
    Apply this to all 3 cards (Pay Bills, New Ticket, View Wallet).
  </action>
  <verify>
    <automated>ls artifacts/web/src/app/admin/layout.tsx && grep "revalidatePath.*\"/admin/upgrade-requests\"" artifacts/web/src/app/admin/upgrade-requests/actions.ts && grep "<Link href=\"/bills\"" artifacts/web/src/app/\(resident\)/layout.tsx && grep "dark:bg-gray-800" artifacts/web/src/app/\(resident\)/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `artifacts/web/src/app/admin/` directory exists (no parentheses)
    - `artifacts/web/src/app/(admin)/` directory does NOT exist
    - Both revalidatePath calls in upgrade-requests/actions.ts use "/admin/upgrade-requests"
    - All 5 bottom nav elements use `<Link>` not `<a>`
    - All 3 quick action cards have `dark:bg-gray-800 dark:border-gray-700`
  </acceptance_criteria>
  <done>Admin pages accessible at /admin/* URLs. Bottom nav uses client-side navigation. Dashboard cards render in dark mode.</done>
</task>

<task type="auto">
  <name>Task 2: Run code formatter and verify build</name>
  <files>All .ts/.tsx files touched by formatter</files>
  <read_first>
    - package.json (root — confirm format script exists)
  </read_first>
  <action>
    STEP 1 - Run formatter:
    Run `pnpm format` from the project root. This runs Prettier across all .ts/.tsx files.
    Expected: ~148 files reformatted.

    STEP 2 - Verify format check passes:
    Run `pnpm format:check` to confirm zero formatting errors remain.

    STEP 3 - Verify the app builds:
    Run `cd artifacts/web && pnpm build` to confirm the admin directory rename did not break imports or routing.
    The build must complete without errors.

    If build fails due to import paths referencing `(admin)`, grep for any remaining references and fix them.
    Check: `grep -r "(admin)" artifacts/web/src/ --include="*.ts" --include="*.tsx"` should return zero results
    (except legitimate content strings, not import paths).
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/scla && pnpm format:check</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm format:check` exits with code 0
    - `pnpm build` in artifacts/web completes without errors
  </acceptance_criteria>
  <done>All code formatted. Build passes. No regressions from admin rename.</done>
</task>

</tasks>

<verification>
1. Admin routing: `ls artifacts/web/src/app/admin/dashboard/page.tsx` succeeds
2. No old directory: `ls artifacts/web/src/app/(admin)/ 2>&1` fails (does not exist)
3. revalidatePath fixed: `grep -c "/admin/upgrade-requests" artifacts/web/src/app/admin/upgrade-requests/actions.ts` returns 2
4. Bottom nav: `grep -c "<Link href" artifacts/web/src/app/\(resident\)/layout.tsx` returns 5 (for bottom nav)
5. Dark mode: `grep -c "dark:bg-gray-800" artifacts/web/src/app/\(resident\)/page.tsx` returns 3
6. Format: `pnpm format:check` passes
</verification>

<success_criteria>
1. Admin sidebar navigation links all resolve to correct pages (no 404s)
2. Admin redirect() calls after mutations land on the correct admin page
3. Bottom nav taps perform client-side navigation (no full page reload)
4. Dashboard quick action cards render correctly in dark mode
5. pnpm format:check passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/32-integration-fixes-polish/32-01-SUMMARY.md`
</output>
