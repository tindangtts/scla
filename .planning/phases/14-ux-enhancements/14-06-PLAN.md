---
phase: 14-ux-enhancements
plan: "06"
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - artifacts/scla/src/components/layout/bottom-nav.tsx
  - artifacts/scla/src/pages/home.tsx
  - artifacts/scla/src/pages/bills.tsx
  - artifacts/scla/src/pages/new-ticket.tsx
  - artifacts/scla/src/pages/star-assist.tsx
  - artifacts/scla/src/pages/bookings.tsx
  - artifacts/scla/src/pages/booking-detail.tsx
  - artifacts/scla/src/pages/login.tsx
  - artifacts/scla/src/pages/register.tsx
  - artifacts/scla/src/pages/discover.tsx
  - artifacts/scla/src/pages/notifications.tsx
autonomous: true
requirements:
  - ENH-01

must_haves:
  truths:
    - "All static UI strings in the app render in Myanmar when language is set to 'my'"
    - "All static UI strings revert to English when language is set to 'en'"
    - "User-generated content (announcements, descriptions, ticket notes) is NOT translated"
    - "Bottom navigation labels switch language"
    - "Page titles, button labels, form labels, and status strings all switch language"
  artifacts:
    - path: "artifacts/scla/src/components/layout/bottom-nav.tsx"
      provides: "Nav labels use t('nav.home') etc."
      contains: "useTranslation"
    - path: "artifacts/scla/src/pages/home.tsx"
      provides: "Home page uses t() for all static strings"
      contains: "useTranslation"
    - path: "artifacts/scla/src/pages/new-ticket.tsx"
      provides: "New ticket form labels use t()"
      contains: "useTranslation"
  key_links:
    - from: "all translated pages"
      to: "artifacts/scla/src/locales/en.json + my.json"
      via: "useTranslation hook, t('key') calls"
      pattern: "useTranslation"
---

<objective>
Apply i18n translations across all frontend pages and components — bottom nav, home, bills, star-assist pages, bookings, login, register, discover, and notifications. Every hardcoded static UI string is replaced with a `t('key')` call using keys defined in Plan 14-01.

Purpose: Completing the i18n wiring so residents actually see Myanmar text in the UI when they have selected the Myanmar language.

Output: Every page renders translated strings based on the active language. User-generated content remains untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-ux-enhancements/14-CONTEXT.md
@.planning/phases/14-ux-enhancements/14-01-SUMMARY.md

<interfaces>
<!-- react-i18next usage pattern -->
```typescript
import { useTranslation } from 'react-i18next';

// Inside component:
const { t } = useTranslation();

// Replace hardcoded strings:
// "Home" --> t('nav.home')
// "My Bills" --> t('bills.title')
// "Submit Ticket" --> t('ticket.new.submit')
```

<!-- DO NOT translate: -->
<!-- - announcement.title, announcement.content (user content) -->
<!-- - booking.facilityName, ticket.title, ticket.description (user data) -->
<!-- - user.name, user.email (user data) -->
<!-- - Dynamic numbers, amounts, dates -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply useTranslation to bottom-nav, home, and login/register pages</name>
  <files>
    artifacts/scla/src/components/layout/bottom-nav.tsx,
    artifacts/scla/src/pages/home.tsx,
    artifacts/scla/src/pages/login.tsx,
    artifacts/scla/src/pages/register.tsx
  </files>
  <read_first>
    - artifacts/scla/src/components/layout/bottom-nav.tsx (full file — replace nav label strings)
    - artifacts/scla/src/pages/home.tsx (full file — replace section titles and labels)
    - artifacts/scla/src/pages/login.tsx (full file — replace form labels and buttons)
    - artifacts/scla/src/pages/register.tsx (full file — replace form labels and buttons)
    - artifacts/scla/src/locales/en.json (check available keys)
  </read_first>
  <action>
    For each file:
    1. Add `import { useTranslation } from 'react-i18next';` at the top.
    2. Add `const { t } = useTranslation();` inside the component function.
    3. Replace all hardcoded static UI strings with `t('key')` calls using the keys from en.json.

    **bottom-nav.tsx:** Replace nav label strings (Home, Bills, StarAssist, Bookings, Profile) with t('nav.home'), t('nav.bills'), t('nav.assist'), t('nav.bookings'), t('nav.profile').

    **home.tsx:** Replace:
    - Section headings ("Quick Actions", "My Bills", "My Tickets", "My Bookings") → t('home.quickActions') etc.
    - Button labels ("View All", "Pay Now") → t('common.viewAll'), t('bills.pay')
    - Empty state messages → appropriate t() keys
    - Status labels → t('common.status.upcoming') etc.
    - DO NOT translate: user.name, unit numbers, amounts, dates, dynamic data

    **login.tsx:** Replace:
    - Form labels (Email, Password) → t('auth.email'), t('auth.password')
    - Button ("Sign In") → t('auth.login')
    - "Don't have an account?" → t('auth.noAccount')
    - Loading state ("Signing in...") → t('auth.signingIn')

    **register.tsx:** Replace:
    - Form labels → t('auth.name'), t('auth.email'), t('auth.password'), t('auth.phone')
    - Button → t('auth.register')
    - Loading state → t('auth.registering')
    - "Already have an account?" → t('auth.hasAccount')
  </action>
  <verify>
    <automated>grep -l "useTranslation" /Users/tindang/workspaces/tts/yoma/scla/artifacts/scla/src/components/layout/bottom-nav.tsx /Users/tindang/workspaces/tts/yoma/scla/artifacts/scla/src/pages/home.tsx /Users/tindang/workspaces/tts/yoma/scla/artifacts/scla/src/pages/login.tsx /Users/tindang/workspaces/tts/yoma/scla/artifacts/scla/src/pages/register.tsx && echo "OK"</automated>
  </verify>
  <done>bottom-nav, home, login, register all import useTranslation and replace hardcoded strings with t() calls; no hardcoded English nav labels or form labels remain.</done>
</task>

<task type="auto">
  <name>Task 2: Apply useTranslation to tickets, bookings, discover, and notifications pages</name>
  <files>
    artifacts/scla/src/pages/star-assist.tsx,
    artifacts/scla/src/pages/new-ticket.tsx,
    artifacts/scla/src/pages/bookings.tsx,
    artifacts/scla/src/pages/booking-detail.tsx,
    artifacts/scla/src/pages/discover.tsx,
    artifacts/scla/src/pages/notifications.tsx,
    artifacts/scla/src/pages/bills.tsx
  </files>
  <read_first>
    - artifacts/scla/src/pages/star-assist.tsx (full file)
    - artifacts/scla/src/pages/new-ticket.tsx (full file — already modified by Plan 14-04)
    - artifacts/scla/src/pages/bookings.tsx (full file — already modified by Plan 14-07)
    - artifacts/scla/src/pages/booking-detail.tsx (full file)
    - artifacts/scla/src/pages/discover.tsx (full file)
    - artifacts/scla/src/pages/notifications.tsx (full file)
    - artifacts/scla/src/pages/bills.tsx (full file)
    - artifacts/scla/src/locales/en.json (check available keys)
  </read_first>
  <action>
    For each file, follow the same pattern: add `import { useTranslation }`, add `const { t } = useTranslation()`, replace hardcoded static strings with t() calls.

    **star-assist.tsx (StarAssist list page):**
    - Page title → t('tickets.title')
    - "New Ticket" button → t('tickets.new')
    - Empty state → t('tickets.noTickets')
    - Status labels (open, in_progress, completed, closed) → t('common.status.open') etc.

    **new-ticket.tsx:**
    - Page title "Raise a Ticket" → t('ticket.new.title')
    - Form labels: Category → t('ticket.new.category'), Specific Issue → t('ticket.new.serviceType'), Short Title → t('ticket.new.shortTitle'), Description → t('ticket.new.description'), Unit Number → t('ticket.new.unit')
    - Submit button: "Submit Ticket" → t('ticket.new.submit'), "Submitting..." → t('ticket.new.submitting')
    - Attachment: "Attach Photo" → t('ticket.new.attachment'), hint → t('ticket.new.attachmentHint')
    - DO NOT translate: category value strings (these are API enum values), placeholder text that is descriptive

    **bookings.tsx:**
    - Page title → t('bookings.title')
    - Tab labels "Facilities" → t('bookings.facilities'), "My Bookings" → t('bookings.myBookings')
    - Empty state → t('bookings.noBookings')

    **booking-detail.tsx:**
    - "Select Date" → t('bookings.selectDate'), "Select Time" → t('bookings.selectTime'), "Today" → t('bookings.today')
    - "Booking Summary" → t('bookings.summary'), "Date" → t('bookings.date'), "Time" → t('bookings.time'), "Total" → t('bookings.total')
    - "Confirm Booking" → t('bookings.confirm'), "Confirming..." → t('bookings.confirming')
    - "Repeat weekly (4 weeks)" → t('bookings.repeatWeekly') — this string is added by Plan 14-07; add it here if 14-07 has not run yet

    **bills.tsx:**
    - Page title → t('bills.title')
    - Status filter labels (Unpaid, Paid) → t('bills.unpaid'), t('bills.paid')
    - Empty state → t('bills.noBills')
    - "Pay Now" → t('bills.pay')

    **discover.tsx:**
    - Page title → t('discover.title')
    - Tab labels → t('discover.announcements'), t('discover.promotions')

    **notifications.tsx:**
    - Page title → t('notifications.title')
    - Empty state text as appropriate
  </action>
  <verify>
    <automated>grep -rl "useTranslation" /Users/tindang/workspaces/tts/yoma/scla/artifacts/scla/src/pages/ | wc -l && echo "pages with useTranslation (expect >= 8)"</automated>
  </verify>
  <done>All listed pages import useTranslation and replace static strings with t() calls; user-generated content (announcement bodies, ticket descriptions) is NOT wrapped in t(); build passes.</done>
</task>

</tasks>

<verification>
- `grep -rl "useTranslation" artifacts/scla/src/pages/` returns 8+ files
- bottom-nav.tsx has t('nav.home') etc. for all nav labels
- No hardcoded "Submit Ticket", "My Bills", "Facilities" strings remain in page files (unless inside dynamic data)
- `pnpm --filter scla build` passes without TypeScript errors
</verification>

<success_criteria>
- All static UI strings in pages and bottom-nav replaced with t() calls
- myanmar translations render when language is 'my' (verifiable by switching language in profile)
- User-generated content (descriptions, names, amounts) not wrapped in t()
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/14-ux-enhancements/14-06-SUMMARY.md`
</output>
