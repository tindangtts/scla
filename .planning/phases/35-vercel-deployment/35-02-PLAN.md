---
phase: 35-vercel-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - DEPLOYMENT.md
  - .planning/STATE.md
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04
  - DEPLOY-05

user_setup:
  - service: vercel
    why: "Host the Next.js app, run daily cron, and serve HTTPS production URL"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase dashboard → Project Settings → API"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase dashboard → API"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase dashboard → API (service role)"
      - name: DATABASE_URL
        source: "Supabase → Connection Pooling (port 6543)"
      - name: NEXT_PUBLIC_VAPID_PUBLIC_KEY
        source: "Generated via web-push (see artifacts/web/.env.local)"
      - name: VAPID_PRIVATE_KEY
        source: "Same as above — paired with public key"
      - name: VAPID_SUBJECT
        source: "mailto: address used for push subscriptions"
      - name: RESEND_API_KEY
        source: "resend.com dashboard (optional)"
      - name: CRON_SECRET
        source: "Local .env.local — copy the existing 64-char hex value"
    dashboard_config:
      - task: "Leave Root Directory at repo root (do NOT set to artifacts/web)"
        location: "Vercel → Project → Settings → General → Root Directory"
      - task: "Confirm Install/Build/Output commands auto-detected from vercel.json"
        location: "Vercel → Project → Settings → Build & Development Settings"
      - task: "Confirm Cron Jobs entry shows /api/cron/bill-overdue-check daily"
        location: "Vercel → Project → Settings → Cron Jobs"

must_haves:
  truths:
    - "Vercel CLI is installed globally and authenticated as the project owner"
    - "Vercel project is linked to the repo root with Next.js detected and crons visible"
    - "All required env vars (9 required, 2 optional/blank) are present in the Vercel project across the targeted environments (Preview + Production)"
    - "Preview deployment builds green and serves /login, /, /admin/login, /admin at HTTPS"
    - "Production deployment on main builds green and serves the same routes over HTTPS"
    - "A manual cron invocation against the production URL returns HTTP 200 with JSON body { processed, errors, total }"
  artifacts:
    - path: ".vercel/project.json"
      provides: "Local link to Vercel project (gitignored) — proves vercel link completed"
      contains: "projectId"
  key_links:
    - from: "Vercel project"
      to: "artifacts/web Next.js build"
      via: "vercel.json build/install commands"
      pattern: "buildCommand.*@workspace/web"
    - from: "Vercel Cron scheduler"
      to: "/api/cron/bill-overdue-check"
      via: "Authorization: Bearer $CRON_SECRET"
      pattern: "authorization.*bearer"
---

<objective>
Execute the interactive portion of the Vercel deploy: install CLI, link project, upload env vars, create preview, smoke test, promote to production, and confirm cron is scheduled. This plan is NOT autonomous — every task before Task 6 requires user interaction (CLI auth, browser OAuth, or dashboard review).

Purpose: Put the SCLA v3.1 build on a stable HTTPS URL with daily cron running. Ship.

Output: A running Vercel production deployment, env vars configured, cron scheduled, preview + prod smoke-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-vercel-deployment/35-CONTEXT.md
@.planning/phases/35-vercel-deployment/35-01-PLAN.md
@DEPLOYMENT.md
@vercel.json
@artifacts/web/.env.example

<interfaces>
<!-- Plan 35-01 committed these — they are the deploy contract. -->

vercel.json at repo root declares:
- installCommand: pnpm install --frozen-lockfile
- buildCommand: pnpm --filter @workspace/web build
- outputDirectory: artifacts/web/.next
- framework: nextjs
- regions: [sin1]
- crons: [{ path: /api/cron/bill-overdue-check, schedule: 0 2 * * * }]

Cron handler (artifacts/web/src/app/api/cron/bill-overdue-check/route.ts) accepts:
- GET with `Authorization: Bearer $CRON_SECRET` (Vercel Cron path)
- POST with `x-cron-secret: $CRON_SECRET` (legacy/manual path)

Env var keys required in Vercel (from artifacts/web/.env.example):
NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY,
DATABASE_URL, NEXT_PUBLIC_VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, VAPID_SUBJECT,
RESEND_API_KEY (optional — can be blank), CRON_SECRET,
NEXT_PUBLIC_WS_URL (leave BLANK — polling fallback), WS_BROADCAST_URL (leave BLANK).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User installs Vercel CLI and logs in</name>
  <files>N/A (environment-level setup, no repo files touched)</files>
  <what-built>Plan 35-01 created vercel.json, the cron handler, DEPLOYMENT.md, and .env.example. Repo is deploy-ready, but the Vercel CLI is not installed locally.</what-built>
  <action>
WAIT for user. User runs the following commands in a terminal (repo root or anywhere — these are global):

```sh
pnpm add -g vercel
vercel --version    # should print a version, not "command not found"
vercel login        # opens browser → sign in with the account that owns the project
vercel whoami       # should print your Vercel username/team
```

Claude MUST NOT run `pnpm add -g` autonomously — global installs change the user's shell environment and `vercel login` requires a browser OAuth round-trip that only the human can complete.

Once the user reports success, Claude confirms by running `vercel whoami` to verify auth is in place before proceeding to Task 2.
  </action>
  <how-to-verify>
Confirm by pasting the output of `vercel whoami` OR typing "logged in as <username>".
  </how-to-verify>
  <verify>
    <automated>vercel whoami</automated>
  </verify>
  <acceptance_criteria>
    - `vercel --version` works
    - `vercel whoami` returns the expected user/team
  </acceptance_criteria>
  <resume-signal>Type "logged in" (or paste whoami output) to continue. Type the error verbatim if either command fails.</resume-signal>
  <done>Vercel CLI authenticated locally; subsequent tasks can invoke `vercel` non-interactively.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User runs vercel link at repo root to create/attach the project</name>
  <files>.vercel/project.json (created by vercel link — gitignored)</files>
  <what-built>CLI is authenticated. Now it needs to know which Vercel project this repo maps to. `vercel link` creates that mapping and writes `.vercel/project.json` (gitignored).</what-built>
  <action>
WAIT for user. User runs from `/Users/tindang/workspaces/tts/yoma/scla`:

```sh
vercel link
```

Prompts:
- **Set up and link?** → yes
- **Which scope?** → select your personal account OR team
- **Link to existing project?** → NO (unless you already created one — answer accordingly)
- **Project name?** → `scla` (or `star-city-living-app` — your choice; lock the decision in STATE.md after)
- **Root directory?** → **press ENTER to accept `./`** (the repo root — do NOT point it at artifacts/web, vercel.json already handles the monorepo paths)

Claude MUST NOT auto-run `vercel link` — it is an interactive prompt flow that needs the user to choose scope + project name + confirm root directory. Scripted answers here risk creating a project in the wrong scope.

After the user completes the flow, Claude verifies by reading `.vercel/project.json` and running `vercel project ls`.
  </action>
  <how-to-verify>
```sh
cat .vercel/project.json    # should show projectId + orgId
vercel project ls           # the project should appear in the list
```

Paste the project name and projectId snippet (first 8 chars is enough) to confirm.
  </how-to-verify>
  <verify>
    <automated>test -f /Users/tindang/workspaces/tts/yoma/scla/.vercel/project.json && grep -q projectId /Users/tindang/workspaces/tts/yoma/scla/.vercel/project.json && vercel project ls</automated>
  </verify>
  <acceptance_criteria>
    - `.vercel/project.json` exists locally and contains `projectId`
    - Project is visible in `vercel project ls`
    - Root Directory is `./` (the repo root) — NOT `artifacts/web`
  </acceptance_criteria>
  <resume-signal>Type "linked: <project-name>" or paste `vercel project ls` output. If the Root Directory was accidentally set to `artifacts/web`, fix it in the dashboard (Settings → General → Root Directory → repo root) before continuing.</resume-signal>
  <done>Vercel project exists and is linked to this repo root (DEPLOY-01 final).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: User sets all required env vars in Vercel</name>
  <files>N/A (Vercel dashboard / CLI only — no local repo files touched)</files>
  <what-built>Project is linked. It now needs the 9 required env vars to build and run. The 2 WS-related vars must be set blank (or omitted) per DEPLOYMENT.md.</what-built>
  <action>
WAIT for user. User chooses Option A (dashboard) or Option B (CLI):

**Option A — Dashboard (recommended first time):**

Go to https://vercel.com/<scope>/<project>/settings/environment-variables and add each of the 9 required keys for BOTH `Preview` and `Production` environments. Source values verbatim from `artifacts/web/.env.local`:

Required:
1. NEXT_PUBLIC_SUPABASE_URL
2. NEXT_PUBLIC_SUPABASE_ANON_KEY
3. SUPABASE_SERVICE_ROLE_KEY
4. DATABASE_URL
5. NEXT_PUBLIC_VAPID_PUBLIC_KEY
6. VAPID_PRIVATE_KEY
7. VAPID_SUBJECT
8. RESEND_API_KEY (optional — blank is fine, disables email)
9. CRON_SECRET (copy the existing 64-char hex from .env.local verbatim)

Do NOT set:
- NEXT_PUBLIC_WS_URL (leave unset or blank — polling fallback)
- WS_BROADCAST_URL (leave unset or blank — polling fallback)

**Option B — CLI (faster for subsequent runs):**

```sh
# For each required key, run:
vercel env add <KEY> production
# paste the value when prompted

# Repeat for "preview" environment (same values):
vercel env add <KEY> preview
```

Claude MUST NOT paste secret values into `vercel env add` on behalf of the user — the user's `.env.local` holds secrets Claude should not be transmitting. The user runs this task.

**Critical:** `CRON_SECRET` in Vercel MUST equal the value in local `.env.local` so manual curl smoke tests work post-deploy.

After the user reports done, Claude verifies by running `vercel env ls production` and `vercel env ls preview` to confirm the key names are present (Vercel redacts values — confirming key presence is sufficient).
  </action>
  <how-to-verify>
```sh
vercel env ls production
vercel env ls preview
```

Both listings should show all 9 required keys (WS_* absent or blank).
  </how-to-verify>
  <verify>
    <automated>vercel env ls production | grep -E "NEXT_PUBLIC_SUPABASE_URL|SUPABASE_SERVICE_ROLE_KEY|DATABASE_URL|VAPID_PRIVATE_KEY|CRON_SECRET"</automated>
  </verify>
  <acceptance_criteria>
    - `vercel env ls production` shows all 9 required keys
    - `vercel env ls preview` shows all 9 required keys
    - CRON_SECRET value in Vercel == CRON_SECRET value in local .env.local
    - NEXT_PUBLIC_WS_URL and WS_BROADCAST_URL are either absent or blank
  </acceptance_criteria>
  <resume-signal>Type "env set" and paste `vercel env ls production` output (values redacted). If any key was typo'd, fix via the dashboard and re-run `vercel env ls`.</resume-signal>
  <done>All required env vars uploaded to Vercel Preview + Production (DEPLOY-02).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Claude creates preview deployment; user smoke-tests it</name>
  <files>N/A (Vercel-side build — no local repo files touched)</files>
  <what-built>Project linked, env vars set. Claude now runs `vercel deploy` (no --prod) to build a preview URL. User verifies the four critical flows work against Supabase before we promote to production.</what-built>
  <action>
Claude runs from repo root:

```sh
vercel deploy
```

This streams build logs and prints a `https://<random>-<project>.vercel.app` URL when done. Claude captures that URL and presents it to the user.

User smoke-tests the preview URL:

1. Open `<preview-url>/login` → resident login page renders with SC mark + brand warmth (v3.1 Phase 33 polish).
2. Log in with a demo resident account → redirects to `/` → dashboard loads with bills list.
3. Open `<preview-url>/admin/login` in a private window → darker admin theme renders.
4. Log in with a demo admin account → redirects to `/admin` → admin dashboard loads.
5. From a terminal, hit the cron endpoint manually (substitute `<preview-url>` and use the same `CRON_SECRET` as Vercel):

```sh
curl -s -X POST "<preview-url>/api/cron/bill-overdue-check" \
  -H "x-cron-secret: $CRON_SECRET" | jq .
```

Expected JSON: `{ "processed": <n>, "errors": 0, "total": <n> }` (HTTP 200).

6. Open a ticket with chat → type a message → verify it appears via polling fallback (may take ~3s to roundtrip).

If any step fails, Claude fetches `vercel logs <deployment-url>` and reports; do NOT proceed to production until all 6 steps pass.
  </action>
  <how-to-verify>
Complete the 6-step smoke test above. Report pass/fail per step.
  </how-to-verify>
  <verify>
    <automated>vercel ls --scope "$(jq -r .orgId /Users/tindang/workspaces/tts/yoma/scla/.vercel/project.json)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Preview build exits 0 (no TypeScript or bundle error in Vercel logs)
    - All 6 smoke tests pass (login, dashboard, admin login, admin dashboard, cron 200, chat polling)
    - No 500s in the server logs during the smoke run
  </acceptance_criteria>
  <resume-signal>Type "preview ok" if all 6 pass. Otherwise paste the failing step number + error message + relevant log excerpt.</resume-signal>
  <done>Preview deployment is functional end-to-end against Supabase (DEPLOY-04).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Claude promotes to production; user confirms live HTTPS URL</name>
  <files>N/A (Vercel-side promotion — no local repo files touched)</files>
  <what-built>Preview is green. Now Claude runs `vercel deploy --prod` to publish to the stable `*.vercel.app` production URL. User confirms the live URL serves traffic and the cron job is registered.</what-built>
  <action>
Claude runs from repo root:

```sh
vercel deploy --prod
```

When it finishes, it prints the canonical production URL (e.g. `https://scla.vercel.app`). Claude captures that URL.

User verifies:

1. Open the production URL in a fresh browser → `/login` renders.
2. Log in as a demo resident → dashboard loads.
3. Log in as a demo admin at `/admin/login` → admin dashboard loads.
4. Open Vercel dashboard → Project → **Cron Jobs** tab → confirm an entry exists for `/api/cron/bill-overdue-check` scheduled `0 2 * * *`. If the tab is empty, the cron is NOT registered — redeploy after confirming vercel.json was included in the deploy.
5. Manually hit the production cron endpoint:

```sh
curl -s -X POST "https://<prod-url>/api/cron/bill-overdue-check" \
  -H "x-cron-secret: $CRON_SECRET" | jq .
```

Expected HTTP 200 + JSON body with `processed`/`errors`/`total` keys.

6. Deployment Protection: if the production URL shows a Vercel auth wall, go to Project → Settings → Deployment Protection and switch to "Only Preview Deployments" (or disable). Production must be publicly reachable per DEPLOY-05.

Claude does NOT promote to prod autonomously — this is a checkpoint:human-verify so the user is explicitly signing off on publishing to the live URL.
  </action>
  <how-to-verify>
Complete the 6-step production verification above. Report pass/fail per step.
  </how-to-verify>
  <verify>
    <automated>vercel ls --prod | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `vercel deploy --prod` exits 0
    - Production URL is publicly reachable over HTTPS (no 401/403 auth wall on /login)
    - Vercel dashboard Cron Jobs tab lists the bill-overdue-check entry
    - Manual curl to the production cron endpoint returns 200
    - Demo resident + admin logins succeed on production
  </acceptance_criteria>
  <resume-signal>Type "prod live: <url>" with the production URL. If the cron is missing from the dashboard, paste the `vercel.json` section from the deployment's "Source" tab and we will redeploy.</resume-signal>
  <done>Production is live on HTTPS with cron scheduled (DEPLOY-03 final, DEPLOY-05 final).</done>
</task>

<task type="auto">
  <name>Task 6: Record the production URL + queue 24h cron verification follow-up</name>
  <files>DEPLOYMENT.md, .planning/STATE.md</files>
  <read_first>
    - /Users/tindang/workspaces/tts/yoma/scla/.vercel/project.json (read scope/projectName for the Preview dashboard link)
    - User's resume-signal from Task 5 (production URL)
    - /Users/tindang/workspaces/tts/yoma/scla/DEPLOYMENT.md (append, do not overwrite)
    - /Users/tindang/workspaces/tts/yoma/scla/.planning/STATE.md (append to Blockers/Concerns, do not overwrite)
  </read_first>
  <action>
Production is live. Update two files:

1. **DEPLOYMENT.md** — append a new H2 section at the bottom named "## Deployed URLs" with a table:

```markdown
## Deployed URLs

| Environment | URL | Region | Deployed |
|-------------|-----|--------|----------|
| Production | https://<prod-url-from-task-5> | sin1 | <YYYY-MM-DD> |
| Preview | https://vercel.com/<scope>/<project> (per-deploy URLs in dashboard) | sin1 | rolling |
```

Replace `<prod-url-from-task-5>` with the actual URL the user reported in Task 5's resume signal. Replace `<scope>/<project>` with the values from `.vercel/project.json` (read via `jq .orgId .vercel/project.json` and the project name from `vercel project ls`). Replace `<YYYY-MM-DD>` with today's date.

2. **.planning/STATE.md** — append this bullet under "## Blockers/Concerns":

```markdown
- Vercel Cron runs once per 24h — verify the first scheduled invocation succeeded by checking Vercel → Project → Cron Jobs → <job> → "Logs" the day AFTER the deploy. If the first invocation 401s, the CRON_SECRET env var does not match what Vercel Cron is signing with; rotate and redeploy.
```

Do NOT wait 24h inside this task — the verification happens out-of-band the next day. This task is documentation bookkeeping only.

DO NOT attempt to fetch the cron run log via CLI today (it won't exist yet if the scheduled time hasn't arrived).
  </action>
  <acceptance_criteria>
    - DEPLOYMENT.md gains a "## Deployed URLs" section with the real production URL (not a placeholder)
    - STATE.md's Blockers/Concerns gains the 24h cron verification note
    - Both files parse as valid Markdown (no broken tables)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Deployed URLs' /Users/tindang/workspaces/tts/yoma/scla/DEPLOYMENT.md && grep -qE 'https://[a-z0-9-]+\.vercel\.app' /Users/tindang/workspaces/tts/yoma/scla/DEPLOYMENT.md && grep -q 'Vercel Cron runs once per 24h' /Users/tindang/workspaces/tts/yoma/scla/.planning/STATE.md && echo ok</automated>
  </verify>
  <done>Production URL is recorded in DEPLOYMENT.md and the 24h cron verification reminder is in STATE.md — phase can be closed.</done>
</task>

</tasks>

<verification>
Phase 35 success conditions checked cumulatively:

1. `.vercel/project.json` exists locally, contains projectId (Task 2).
2. `vercel env ls production` shows all 9 required keys (Task 3).
3. `vercel deploy` (preview) produces a working URL; all 6 smoke steps pass (Task 4).
4. `vercel deploy --prod` produces the canonical URL; cron visible in dashboard; manual curl → 200 (Task 5).
5. DEPLOYMENT.md + STATE.md updated with the production URL and 24h cron note (Task 6).
</verification>

<success_criteria>
- All 8 Phase 35 requirements closed:
  - DEPLOY-01: Vercel project linked with monorepo config (35-01 vercel.json + 35-02 Task 2)
  - DEPLOY-02: Env vars set in Vercel (Task 3)
  - DEPLOY-03: Cron scheduled + endpoint working (35-01 cron handler + Task 5)
  - DEPLOY-04: Preview smoke-tested (Task 4)
  - DEPLOY-05: Production reachable on HTTPS (Task 5)
  - DEPLOY-06: Replit config removed (35-01 Task 3)
  - WS-01: Polling fallback documented + verified in chat smoke test (Task 4 step 6)
  - WS-02: Supabase Realtime deferred to ENH-04 (documented in 35-01 DEPLOYMENT.md)
- Production SCLA app reachable at a `*.vercel.app` URL; residents can log in and load their dashboards.
</success_criteria>

<output>
After completion, create `.planning/phases/35-vercel-deployment/35-02-SUMMARY.md` covering:
- Final production URL
- Vercel project name + scope (redact IDs deemed sensitive)
- Env vars set (names only, no values)
- Smoke test results from Task 4 + Task 5 (pass/fail per step)
- Any dashboard tweaks made (e.g. Deployment Protection toggled, Root Directory corrected)
- Pointer to the 24h cron verification note in STATE.md (Task 6)
- Any follow-ups (e.g. custom domain — deferred per ROADMAP "Out of Scope")
</output>
