---
phase: 18-developer-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/api-server/src/seed.ts
autonomous: true
requirements:
  - DX-01
  - DX-02
  - DX-03

must_haves:
  truths:
    - "Running `pnpm run seed` once creates demo accounts, invoices, tickets, bookings, and announcements"
    - "Running `pnpm run seed` a second time completes without errors or duplicates"
    - "A developer can reset the database by re-running the seed command"
  artifacts:
    - path: "artifacts/api-server/src/seed.ts"
      provides: "Idempotent seed script with upsert logic"
      contains: "onConflictDoNothing"
  key_links:
    - from: "artifacts/api-server/src/seed.ts"
      to: "@workspace/db"
      via: "Drizzle ORM insert with onConflictDoNothing"
      pattern: "onConflictDoNothing"
---

<objective>
Make the seed script idempotent and complete with all entity types including bookings.

Purpose: DX-01, DX-02, DX-03 — Developers need a single command that produces a known database state, safe to re-run without duplicates or errors.

Output: Updated `artifacts/api-server/src/seed.ts` that uses upsert/onConflictDoNothing patterns instead of the current "check-and-bail" approach.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@artifacts/api-server/src/seed.ts
@lib/db/src/schema/index.ts
@lib/db/src/schema/users.ts
@lib/db/src/schema/bookings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite seed.ts to be fully idempotent with all entity types</name>
  <files>artifacts/api-server/src/seed.ts</files>
  <read_first>
    - artifacts/api-server/src/seed.ts (current implementation — uses check-and-bail pattern)
    - lib/db/src/schema/index.ts (all table exports)
    - lib/db/src/schema/users.ts (User schema with email unique constraint)
    - lib/db/src/schema/bookings.ts (Booking schema — currently not seeded)
    - lib/db/src/schema/invoices.ts (Invoice schema for conflict target)
    - lib/db/src/schema/tickets.ts (Ticket schema for conflict target)
    - lib/db/src/schema/staff_users.ts (Staff schema for conflict target)
    - lib/db/src/schema/facilities.ts (Facility schema for conflict target)
  </read_first>
  <action>
Rewrite `artifacts/api-server/src/seed.ts` with these changes:

1. **Replace check-and-bail with onConflictDoNothing on every insert.** The current pattern checks `existingUsers.length > 0` and returns early — this means if a partial seed happened, the remaining data is never created. Instead, every `db.insert(table).values(...)` call should chain `.onConflictDoNothing()` using the natural unique constraint for each table:
   - `usersTable` → conflict on `email` (unique constraint)
   - `staffUsersTable` → conflict on `email` (unique constraint)
   - `invoicesTable` → conflict on `invoiceNumber` (unique constraint)
   - `ticketsTable` → conflict on `ticketNumber` (unique constraint)
   - `facilitiesTable` → conflict on `name` (unique — seed by name)
   - `bookingsTable` → conflict on `bookingNumber` (unique constraint)
   - For tables without obvious unique constraints (announcements, promotions, FAQs, info categories, info articles, notifications), use a two-step approach: query count first, only insert if count is 0 for that specific table. This is acceptable because these are reference/content tables with no natural key.

2. **Use deterministic IDs for users/staff so downstream inserts can reference them.** Instead of relying on `.returning()` to get auto-generated UUIDs, assign fixed UUIDs for seed accounts so the script works identically every run:
   ```typescript
   const SEED_IDS = {
     guestUser: "00000000-0000-4000-8000-000000000001",
     residentUser: "00000000-0000-4000-8000-000000000002",
     guestUser2: "00000000-0000-4000-8000-000000000003",
     guestUser3: "00000000-0000-4000-8000-000000000004",
     adminStaff: "00000000-0000-4000-8000-000000000101",
     contentStaff: "00000000-0000-4000-8000-000000000102",
     supportStaff: "00000000-0000-4000-8000-000000000103",
   } as const;
   ```
   Pass `id: SEED_IDS.xxx` in each user/staff insert values.

3. **Add bookings seed data** (currently missing — needed for DX-02). After facilities are inserted, query the first facility to get its ID (or use a fixed facility ID), then insert 2-3 bookings:
   - One upcoming booking for the resident user at the Swimming Pool
   - One completed booking for the resident user at Tennis Court A
   - One cancelled booking for the resident user at the Gym
   Use deterministic booking numbers: "BK-0001", "BK-0002", "BK-0003".

4. **Remove the early-return guards** (`if (existingUsers.length > 0) return`). Each section should run independently using onConflictDoNothing.

5. **Keep all existing seed data content** (announcements text, invoice line items, ticket descriptions, etc.). Do not change the actual data — only change the insertion strategy.

6. **Add a `seedBookings` function** following the same pattern as the existing `seedStaffUsers` function. It should insert bookings with facility IDs looked up from the seeded facilities by name.

7. **Print summary at the end** showing what was seeded (keep existing console.log output for demo account credentials).
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/scla && grep -c "onConflictDoNothing" artifacts/api-server/src/seed.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "onConflictDoNothing" artifacts/api-server/src/seed.ts` returns at least 5 matches (users, staff, invoices, tickets, facilities, bookings)
    - `grep "SEED_IDS" artifacts/api-server/src/seed.ts` returns matches showing deterministic UUID usage
    - `grep "bookingsTable" artifacts/api-server/src/seed.ts` returns matches showing bookings are seeded
    - `grep "BK-000" artifacts/api-server/src/seed.ts` returns matches for deterministic booking numbers
    - No occurrence of `if (existingUsers.length > 0)` early return pattern remains
    - `pnpm run typecheck` passes in the api-server package
  </acceptance_criteria>
  <done>Seed script is idempotent via onConflictDoNothing with deterministic IDs, seeds all entity types including bookings, and can be re-run safely without duplicates or errors.</done>
</task>

</tasks>

<verification>
1. `grep -c "onConflictDoNothing" artifacts/api-server/src/seed.ts` returns >= 5
2. `grep "bookingsTable" artifacts/api-server/src/seed.ts` shows bookings are imported and used
3. `grep -c "SEED_IDS" artifacts/api-server/src/seed.ts` shows deterministic ID usage
4. `cd artifacts/api-server && pnpm run typecheck` passes
</verification>

<success_criteria>
- Seed script uses onConflictDoNothing for all tables with unique constraints
- Deterministic UUIDs assigned for all user/staff seed accounts
- Bookings are seeded (3 bookings with different statuses)
- No early-return bail-out pattern remains
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/18-developer-foundation/18-01-SUMMARY.md`
</output>
