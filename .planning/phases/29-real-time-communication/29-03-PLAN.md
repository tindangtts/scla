---
phase: 29-real-time-communication
plan: 03
type: execute
wave: 2
depends_on:
  - 29-01
  - 29-02
files_modified:
  - artifacts/web/src/components/notification-bell.tsx
  - artifacts/web/src/app/(resident)/layout.tsx
  - artifacts/web/src/app/(resident)/star-assist/[id]/actions.ts
  - artifacts/web/src/app/(admin)/tickets/[id]/actions.ts
  - artifacts/web/src/app/api/tickets/[id]/messages/route.ts
autonomous: true
requirements:
  - COMM-05

must_haves:
  truths:
    - "Notification bell in resident header shows unread count badge"
    - "Badge disappears when user views notifications page"
    - "Creating a ticket message triggers notification to the other party"
    - "Admin updating ticket status triggers notification to the resident"
  artifacts:
    - path: "artifacts/web/src/components/notification-bell.tsx"
      provides: "Client component with bell icon and unread badge"
    - path: "artifacts/web/src/app/(resident)/star-assist/[id]/actions.ts"
      provides: "Server action for sending chat messages with notification trigger"
    - path: "artifacts/web/src/app/(admin)/tickets/[id]/actions.ts"
      provides: "Admin ticket actions with notification triggers on status change"
  key_links:
    - from: "notification-bell.tsx"
      to: "/api/notifications/unread-count"
      via: "Periodic fetch or server component data"
      pattern: "fetch.*notifications"
    - from: "actions.ts (admin)"
      to: "notifications.ts"
      via: "notifyTicketUpdate call on status change"
      pattern: "notifyTicketUpdate"
    - from: "route.ts (messages POST)"
      to: "notifications.ts"
      via: "notifyNewMessage after message insert"
      pattern: "notifyNewMessage"
---

<objective>
In-app notification bell with unread badge and wire notification triggers into existing actions.

Purpose: Residents see an unread notification count in the header, and all relevant actions (ticket status changes, new messages) create notifications so the bell count stays accurate.
Output: NotificationBell component in header, notification triggers wired into ticket and message actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-real-time-communication/29-CONTEXT.md
@.planning/phases/29-real-time-communication/29-01-SUMMARY.md
@.planning/phases/29-real-time-communication/29-02-SUMMARY.md

@artifacts/web/src/app/(resident)/layout.tsx
@artifacts/web/src/lib/queries/notifications.ts
@artifacts/web/src/lib/notifications.ts
@artifacts/web/src/app/(admin)/tickets/[id]/actions.ts
@artifacts/web/src/app/api/tickets/[id]/messages/route.ts

<interfaces>
From artifacts/web/src/lib/queries/notifications.ts:
```typescript
export async function getUnreadCount(userId: string): Promise<number>;
export async function markAsRead(notificationId: string, userId: string): Promise<void>;
export async function markAllAsRead(userId: string): Promise<void>;
```

From artifacts/web/src/lib/notifications.ts (created in Plan 02):
```typescript
export async function notifyBillOverdue(userId: string, invoiceNumber: string, amount: number): Promise<void>;
export async function notifyTicketUpdate(userId: string, ticketId: string, ticketNumber: string, newStatus: string): Promise<void>;
export async function notifyNewMessage(userId: string, ticketId: string, ticketNumber: string): Promise<void>;
```

From artifacts/web/src/lib/auth.ts:
```typescript
export async function requireAuth(); // returns Supabase User
export async function requireAdmin(); // returns { user, staff }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notification bell component and unread count API</name>
  <files>
    artifacts/web/src/components/notification-bell.tsx,
    artifacts/web/src/app/api/notifications/unread-count/route.ts,
    artifacts/web/src/app/(resident)/layout.tsx
  </files>
  <read_first>
    artifacts/web/src/app/(resident)/layout.tsx,
    artifacts/web/src/lib/queries/notifications.ts,
    artifacts/web/src/lib/auth.ts,
    artifacts/web/src/lib/db.ts
  </read_first>
  <action>
1. Create `artifacts/web/src/app/api/notifications/unread-count/route.ts`:
   - GET handler: Auth via createClient/getUser. Look up userId from usersTable by email. Call getUnreadCount(userId) from @/lib/queries/notifications. Return JSON `{ count: number }`.

2. Create `artifacts/web/src/components/notification-bell.tsx` — "use client" component:
   - Renders a bell icon (use an inline SVG bell — no external icon library needed). Links to /notifications.
   - On mount and every 30 seconds, fetches GET /api/notifications/unread-count. Stores count in state.
   - If count > 0, shows a small red circle badge overlaid on the bell with the count number (max display "9+" for counts > 9).
   - Badge uses absolute positioning: relative container on bell, absolute top-right for badge.
   - Clicking the bell navigates to /notifications (use Next.js Link).
   - Cleanup: clear interval on unmount.

3. Update `artifacts/web/src/app/(resident)/layout.tsx`:
   - Import NotificationBell from @/components/notification-bell
   - Add `<NotificationBell />` in the header, between the displayName span and the logout form. The bell should be white (text-white) to match the blue header.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/scla && pnpm --filter web build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - NotificationBell renders a bell icon with unread count badge in the resident header
    - Unread count API returns the correct count from DB
    - Badge shows red circle with count, disappears when count is 0
    - Bell links to /notifications page
    - Count refreshes every 30 seconds
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire notification triggers into ticket and message actions</name>
  <files>
    artifacts/web/src/app/api/tickets/[id]/messages/route.ts,
    artifacts/web/src/app/(admin)/tickets/[id]/actions.ts,
    artifacts/web/src/app/(resident)/star-assist/[id]/actions.ts
  </files>
  <read_first>
    artifacts/web/src/app/api/tickets/[id]/messages/route.ts,
    artifacts/web/src/app/(admin)/tickets/[id]/actions.ts,
    artifacts/web/src/lib/notifications.ts,
    lib/db/src/schema/ticket_messages.ts,
    artifacts/web/src/lib/queries/tickets.ts
  </read_first>
  <action>
1. Update `artifacts/web/src/app/api/tickets/[id]/messages/route.ts` (POST handler):
   - After successfully inserting the message, determine the OTHER party's userId:
     - If senderType is "resident": the ticket's assignedTo field is the staff userId. If no assignedTo, skip notification.
     - If senderType is "staff": the ticket's userId (the resident who created it) is the recipient.
   - To get ticket info: query ticketsTable by ticketId to get userId, assignedTo, and ticketNumber.
   - Call `notifyNewMessage(recipientUserId, ticketId, ticketNumber)` from @/lib/notifications. Fire-and-forget (don't await, or await but don't let errors block the response).

2. Update `artifacts/web/src/app/(admin)/tickets/[id]/actions.ts`:
   - Read the existing updateTicketStatus server action.
   - After the status update DB query succeeds, look up the ticket to get userId and ticketNumber.
   - Call `notifyTicketUpdate(ticket.userId, ticket.id, ticket.ticketNumber, newStatus)` from @/lib/notifications. Fire-and-forget.
   - Import notifyTicketUpdate at top of file.

3. Create or update `artifacts/web/src/app/(resident)/star-assist/[id]/actions.ts`:
   - If this file doesn't exist, create it as a "use server" module.
   - Export a `sendTicketMessage` server action that accepts `{ ticketId: string, content: string }`:
     - Auth via createClient/getUser, look up DB user.
     - Insert into ticketMessagesTable with senderType "resident", senderId as dbUser.id.
     - Look up ticket to get ticketNumber and assignedTo.
     - If assignedTo exists, call notifyNewMessage(assignedTo, ticketId, ticketNumber).
     - Return the created message.
   - This action can optionally be used by the chat component as an alternative to the REST POST (but the REST route also works — either path is fine, keep both).
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/scla && pnpm --filter web build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - POST /api/tickets/[id]/messages triggers notifyNewMessage to the other party
    - Admin updateTicketStatus triggers notifyTicketUpdate to the resident
    - Resident sendTicketMessage server action creates message + triggers notification
    - All notification triggers are fire-and-forget (don't block the primary action)
    - Build succeeds
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter web build` succeeds
2. Notification bell appears in resident header with correct unread count
3. Sending a message from admin creates a notification for the resident (bell count increments)
4. Changing ticket status from admin creates a notification for the resident
5. Visiting /notifications page and marking as read causes bell count to decrease
</verification>

<success_criteria>
- NotificationBell shows accurate unread count in resident header, refreshes every 30s
- Admin ticket status change creates in-app notification + push + email for resident
- New chat message creates in-app notification + push for the other party
- All notifications are fire-and-forget and don't block primary actions
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/29-real-time-communication/29-03-SUMMARY.md`
</output>
