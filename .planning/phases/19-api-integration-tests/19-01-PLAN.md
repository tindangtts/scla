---
phase: 19-api-integration-tests
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/api-server/package.json
  - artifacts/api-server/src/__tests__/helpers.ts
  - artifacts/api-server/src/__tests__/auth.integration.test.ts
  - artifacts/api-server/src/__tests__/invoices.integration.test.ts
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "Auth integration tests verify register, login, /me, and upgrade-request endpoints with correct status codes and response shapes"
    - "Bill integration tests verify list, detail, summary, and pay endpoints for a seeded resident"
    - "Tests run via `pnpm run test` without a real database connection"
  artifacts:
    - path: "artifacts/api-server/src/__tests__/helpers.ts"
      provides: "Shared test utilities — supertest app instance, JWT token generator, db mock helpers"
      exports: ["app", "createTestToken", "mockDb"]
    - path: "artifacts/api-server/src/__tests__/auth.integration.test.ts"
      provides: "Auth endpoint integration tests"
      min_lines: 80
    - path: "artifacts/api-server/src/__tests__/invoices.integration.test.ts"
      provides: "Invoice/bill endpoint integration tests"
      min_lines: 60
  key_links:
    - from: "artifacts/api-server/src/__tests__/helpers.ts"
      to: "artifacts/api-server/src/app.ts"
      via: "supertest wrapping Express app"
      pattern: "supertest.*app"
    - from: "artifacts/api-server/src/__tests__/auth.integration.test.ts"
      to: "/api/auth/*"
      via: "supertest HTTP calls"
      pattern: "request.*post.*auth"
    - from: "artifacts/api-server/src/__tests__/invoices.integration.test.ts"
      to: "/api/invoices/*"
      via: "supertest HTTP calls"
      pattern: "request.*get.*invoices"
---

<objective>
Install supertest, create shared integration test helpers, and write integration tests for auth endpoints (register, login, /me, upgrade) and bill endpoints (list, detail, summary, pay).

Purpose: Establish the integration test infrastructure and cover the two most critical API surface areas — authentication and billing.
Output: supertest installed, shared helpers file, 2 integration test suites passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-developer-foundation/18-02-SUMMARY.md

@artifacts/api-server/vitest.config.ts
@artifacts/api-server/src/__test-setup.ts
@artifacts/api-server/src/app.ts
@artifacts/api-server/src/routes/auth.ts
@artifacts/api-server/src/routes/invoices.ts
@artifacts/api-server/src/lib/jwt.ts
@artifacts/api-server/src/lib/auth-middleware.ts
@artifacts/api-server/src/seed.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/jwt.ts:
```typescript
export interface TokenPayload { userId: string; userType: string; exp: number; }
export function sign(payload: Omit<TokenPayload, "exp">): string;
export function verify(token: string): TokenPayload | null;
```

From src/lib/auth-middleware.ts:
```typescript
export interface AuthenticatedRequest extends Request { user: typeof usersTable.$inferSelect; }
export function requireAuth(req: Request, res: Response, next: NextFunction): Promise<void>;
export function requireAdmin(req: Request, res: Response): AdminTokenPayload | null;
```

From src/routes/index.ts — all routes are under /api prefix:
- POST /api/auth/register — { name, email, phone, password } → 201 { token, user }
- POST /api/auth/login — { email, password } → 200 { token, user }
- GET /api/auth/me — Bearer token → 200 { user }
- POST /api/auth/upgrade — Bearer token + { unitNumber, residentId, developmentName } → 200 { request }
- GET /api/invoices — Bearer token → 200 [ invoices ]
- GET /api/invoices/summary — Bearer token → 200 { totalOutstanding, ... }
- GET /api/invoices/:id — Bearer token → 200 { invoice }
- POST /api/invoices/:id/pay — Bearer token + { paymentMethod, invoiceIds } → 200 { sessionId, ... }

Seed data SEED_IDS:
- residentUser: "00000000-0000-4000-8000-000000000002" (email: resident@starcity.com)
- guestUser: "00000000-0000-4000-8000-000000000001" (email: demo@starcity.com)
- adminStaff: "00000000-0000-4000-8000-000000000101" (email: admin@starcity.com)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install supertest and create shared integration test helpers</name>
  <files>artifacts/api-server/package.json, artifacts/api-server/src/__tests__/helpers.ts</files>
  <read_first>
    - artifacts/api-server/package.json (current deps)
    - artifacts/api-server/vitest.config.ts (test glob pattern)
    - artifacts/api-server/src/__test-setup.ts (existing env setup)
    - artifacts/api-server/src/app.ts (Express app export)
    - artifacts/api-server/src/lib/jwt.ts (sign function for token generation)
  </read_first>
  <action>
1. Install supertest: `cd artifacts/api-server && pnpm add -D supertest @types/supertest`

2. Create `artifacts/api-server/src/__tests__/helpers.ts` with:
   - Import supertest and wrap the Express app: `import request from "supertest"; import app from "../app.js"; export { request, app };`
   - Export a `createTestToken(userId: string, userType: string)` function that calls `jwt.sign({ userId, userType })` to produce a valid Bearer token for tests
   - Export a `createAdminToken(staffId: string, role: string)` function that creates an admin JWT (the admin token format uses `{ staffId, role, ctx: "admin" }` — check auth-middleware.ts for the exact shape)
   - Export SEED_IDS constant matching the values from seed.ts (copy the exact UUIDs)
   - Export a `authHeader(token: string)` helper that returns `{ Authorization: "Bearer " + token }`

3. The vitest.config.ts glob `src/**/__tests__/**/*.test.ts` already covers `src/__tests__/` directory — no config change needed. Note: the helpers.ts file does NOT end in .test.ts so it won't be picked up as a test.

IMPORTANT: Integration tests will mock `@workspace/db` at the module level using vi.mock (same pattern as Phase 18 unit tests). This avoids needing a real database. The helpers file should document this pattern but NOT do the mocking itself — each test file will mock db as needed for its specific route behavior.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/scla/artifacts/api-server && node -e "require('supertest')" 2>&1 || npx tsc --noEmit src/__tests__/helpers.ts 2>&1; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - artifacts/api-server/package.json contains "supertest" in devDependencies
    - artifacts/api-server/src/__tests__/helpers.ts exports: request, app, createTestToken, createAdminToken, SEED_IDS, authHeader
    - helpers.ts imports from "../app.js" and "../lib/jwt.js"
  </acceptance_criteria>
  <done>supertest installed, helpers.ts exports app wrapper, token generators, and seed ID constants</done>
</task>

<task type="auto">
  <name>Task 2: Write auth endpoint integration tests (TEST-01)</name>
  <files>artifacts/api-server/src/__tests__/auth.integration.test.ts</files>
  <read_first>
    - artifacts/api-server/src/routes/auth.ts (all auth endpoints)
    - artifacts/api-server/src/lib/auth-middleware.ts (requireAuth, requireAdmin)
    - artifacts/api-server/src/lib/password.ts (hashPasswordBcrypt, verifyPassword)
    - artifacts/api-server/src/__tests__/helpers.ts (just created in Task 1)
    - artifacts/api-server/src/lib/__tests__/auth-middleware.test.ts (Phase 18 mock pattern reference)
  </read_first>
  <action>
Create `artifacts/api-server/src/__tests__/auth.integration.test.ts` with vi.mock for `@workspace/db` (mock the db object with chainable select/insert/update/delete methods that return controlled data).

Use the INLINE vi.fn() pattern from Phase 18 (NOT outer variable references) to avoid hoisting ReferenceError.

Test cases for POST /api/auth/register:
- Returns 400 when required fields missing (no body)
- Returns 400 when password too short (< 8 chars)
- Returns 400 when email already exists (mock db.select to return existing user)
- Returns 201 with token and user object on valid registration (mock db.select empty + db.insert returning new user)
- Response user object has expected shape: { id, name, email, phone, userType, unitNumber, residentId, estateId, developmentName, upgradeStatus, createdAt }

Test cases for POST /api/auth/login:
- Returns 400 when email/password missing
- Returns 401 when user not found (mock db.select empty)
- Returns 401 when password invalid (mock db.select returning user, but verifyPassword fails — need to mock password.ts)
- Returns 200 with token and user on valid login

Test cases for GET /api/auth/me:
- Returns 401 when no Authorization header
- Returns 200 with user object when valid token provided (mock db.select to return user matching token userId)

Test cases for POST /api/auth/upgrade:
- Returns 401 without auth
- Returns 400 when required fields missing
- Returns 200 with upgrade request on valid submission (mock db.update + db.insert returning request)

IMPORTANT: Mock the rate limiter module (`../lib/rate-limiter.js`) to be a passthrough so tests aren't rate-limited. Use `vi.mock("../lib/rate-limiter.js", () => ({ authRateLimiter: (_req: any, _res: any, next: any) => next() }))`.

Also mock `../lib/password.js` to avoid real bcrypt in tests (slow). Return predictable values: hashPasswordBcrypt returns "hashed", verifyPassword returns true/false based on test scenario.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/scla/artifacts/api-server && npx vitest run src/__tests__/auth.integration.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: artifacts/api-server/src/__tests__/auth.integration.test.ts
    - Contains describe blocks for "POST /api/auth/register", "POST /api/auth/login", "GET /api/auth/me", "POST /api/auth/upgrade"
    - At least 12 test cases (it/test blocks)
    - All tests pass with `npx vitest run`
    - Uses vi.mock for @workspace/db, rate-limiter, and password modules
  </acceptance_criteria>
  <done>Auth integration tests cover register (validation + success), login (validation + auth failure + success), /me (no auth + success), and upgrade (no auth + validation + success) — all passing</done>
</task>

<task type="auto">
  <name>Task 3: Write invoice/bill endpoint integration tests (TEST-02)</name>
  <files>artifacts/api-server/src/__tests__/invoices.integration.test.ts</files>
  <read_first>
    - artifacts/api-server/src/routes/invoices.ts (all invoice endpoints)
    - artifacts/api-server/src/__tests__/helpers.ts (test utilities)
    - artifacts/api-server/src/__tests__/auth.integration.test.ts (reference mock pattern from Task 2)
  </read_first>
  <action>
Create `artifacts/api-server/src/__tests__/invoices.integration.test.ts` with vi.mock for `@workspace/db`.

Mock the auth middleware to inject a fake resident user (SEED_IDS.residentUser) so tests focus on invoice logic, not auth. Two approaches — either mock `../lib/auth-middleware.js` to call next() with req.user set, OR create a valid JWT token via helpers.ts and mock only the db user lookup. Prefer the token approach (more realistic integration test).

Use mock invoice data matching the seed.ts shape:
```typescript
const MOCK_INVOICES = [
  { id: "inv-1", invoiceNumber: "INV-2026-04-001", userId: SEED_IDS.residentUser, unitNumber: "A-12-03", category: "Service Charge", description: "Monthly Service Charge", issueDate: "2026-04-01", dueDate: "2026-04-15", totalAmount: "285000", paidAmount: "0", status: "unpaid", month: "2026-04", lineItems: [...] },
  { id: "inv-2", invoiceNumber: "INV-2026-03-001", userId: SEED_IDS.residentUser, unitNumber: "A-12-03", category: "Service Charge", description: "March Service Charge", issueDate: "2026-03-01", dueDate: "2026-03-15", totalAmount: "285000", paidAmount: "285000", status: "paid", month: "2026-03", lineItems: [...] },
];
```

Test cases for GET /api/invoices (list):
- Returns 401 without auth
- Returns 200 with array of invoices for authenticated resident
- Each invoice has expected shape: { id, invoiceNumber, unitNumber, category, totalAmount (number), paidAmount (number), status, month, lineItems }

Test cases for GET /api/invoices/summary:
- Returns 200 with summary object: { totalOutstanding, totalThisMonth, unpaidCount, partiallyPaidCount, lastPaymentDate, lastPaymentAmount }
- totalOutstanding correctly sums unpaid amounts

Test cases for GET /api/invoices/:id (detail):
- Returns 404 for non-existent invoice
- Returns 200 with invoice detail for valid ID

Test cases for POST /api/invoices/:id/pay:
- Returns 400 when paymentMethod or invoiceIds missing
- Returns 200 with payment session { sessionId, paymentMethod, totalAmount, redirectUrl, status: "pending" }
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/scla/artifacts/api-server && npx vitest run src/__tests__/invoices.integration.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: artifacts/api-server/src/__tests__/invoices.integration.test.ts
    - Contains describe blocks for "GET /api/invoices", "GET /api/invoices/summary", "GET /api/invoices/:id", "POST /api/invoices/:id/pay"
    - At least 8 test cases
    - All tests pass with `npx vitest run`
    - Asserts response shapes match the mapInvoice output format (totalAmount as number, not string)
  </acceptance_criteria>
  <done>Invoice integration tests cover list (auth + success + shape), summary (correct calculations), detail (404 + success), and pay (validation + success) — all passing</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions:
```bash
cd artifacts/api-server && npx vitest run
```
All existing Phase 18 unit tests (23) plus new integration tests should pass.
</verification>

<success_criteria>
- supertest installed in api-server devDependencies
- Shared test helpers file provides token generation and seed IDs
- Auth integration tests cover register, login, /me, upgrade with validation and success cases
- Invoice integration tests cover list, summary, detail, pay with validation and success cases
- All tests pass via `pnpm run test` in api-server
</success_criteria>

<output>
After completion, create `.planning/phases/19-api-integration-tests/19-01-SUMMARY.md`
</output>
