---
phase: 31-testing-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/web/vitest.config.ts
  - artifacts/web/src/lib/__tests__/auth.test.ts
  - artifacts/web/src/lib/__tests__/notifications.test.ts
  - artifacts/web/src/lib/__tests__/push.test.ts
  - artifacts/web/src/app/api/tickets/[id]/messages/__tests__/route.test.ts
  - artifacts/web/src/app/api/notifications/unread-count/__tests__/route.test.ts
  - artifacts/web/src/app/api/cron/bill-overdue-check/__tests__/route.test.ts
  - artifacts/web/src/app/api/push/subscribe/__tests__/route.test.ts
  - artifacts/web/package.json
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "Vitest runs unit tests for auth helpers (getCurrentUser, requireAuth, requireAdmin) with mocked Supabase"
    - "Vitest runs unit tests for notification helpers (notifyBillOverdue, notifyTicketUpdate, notifyNewMessage) with mocked DB and push"
    - "Integration tests exercise API route handlers (messages, unread-count, cron, push subscribe) with mocked dependencies"
    - "All tests pass via a single pnpm command from the root"
  artifacts:
    - path: "artifacts/web/vitest.config.ts"
      provides: "Vitest configuration with @/ path alias resolution"
    - path: "artifacts/web/src/lib/__tests__/auth.test.ts"
      provides: "Unit tests for auth middleware functions"
      min_lines: 50
    - path: "artifacts/web/src/lib/__tests__/notifications.test.ts"
      provides: "Unit tests for notification trigger functions"
      min_lines: 40
    - path: "artifacts/web/src/app/api/tickets/[id]/messages/__tests__/route.test.ts"
      provides: "Integration tests for ticket messages API"
      min_lines: 60
  key_links:
    - from: "artifacts/web/vitest.config.ts"
      to: "artifacts/web/tsconfig.json"
      via: "path alias @/ resolved to ./src/*"
      pattern: "resolve.*alias.*@"
    - from: "artifacts/web/package.json"
      to: "vitest"
      via: "test script"
      pattern: "\"test\": \"vitest"
---

<objective>
Set up Vitest for the Next.js app and write unit tests for auth/notification helpers plus integration tests for all API route handlers.

Purpose: Establish test infrastructure and cover the core business logic (auth guards, notification dispatch) and API routes (messages, unread-count, cron, push subscribe) — matching v2.1 unit+integration coverage in the new Next.js architecture.
Output: Vitest config, unit test files for lib/, integration test files for API routes, test script in package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@artifacts/web/src/lib/auth.ts
@artifacts/web/src/lib/notifications.ts
@artifacts/web/src/lib/push.ts
@artifacts/web/src/app/api/tickets/[id]/messages/route.ts
@artifacts/web/src/app/api/notifications/unread-count/route.ts
@artifacts/web/src/app/api/cron/bill-overdue-check/route.ts
@artifacts/web/src/app/api/push/subscribe/route.ts
@artifacts/web/package.json
@artifacts/web/tsconfig.json

<interfaces>
<!-- Key types and contracts the executor needs -->

From artifacts/web/src/lib/auth.ts:
```typescript
export async function getCurrentUser(): Promise<{ user: User | null }>;
export async function requireAuth(): Promise<User>;  // redirects if no user
export async function requireAdmin(): Promise<{ user: User; staff: StaffUser }>;  // redirects if no user or not staff
```

From artifacts/web/src/lib/notifications.ts:
```typescript
export async function notifyBillOverdue(userId: string, invoiceNumber: string, amount: number): Promise<void>;
export async function notifyTicketUpdate(userId: string, ticketId: string, ticketNumber: string, newStatus: string): Promise<void>;
export async function notifyNewMessage(userId: string, ticketId: string, ticketNumber: string): Promise<void>;
```

From artifacts/web/src/lib/push.ts:
```typescript
export async function sendPushToUser(userId: string, payload: { title: string; body: string; url: string }): Promise<void>;
```

API Routes:
- GET/POST /api/tickets/[id]/messages — auth via Supabase, returns/creates ticket messages
- GET /api/notifications/unread-count — auth via Supabase, returns { count: number }
- POST /api/cron/bill-overdue-check — CRON_SECRET header auth, processes overdue invoices
- POST /api/push/subscribe — auth via Supabase, stores push subscription
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Vitest config and unit tests for auth + notification + push helpers</name>
  <files>
    artifacts/web/vitest.config.ts
    artifacts/web/package.json
    artifacts/web/src/lib/__tests__/auth.test.ts
    artifacts/web/src/lib/__tests__/notifications.test.ts
    artifacts/web/src/lib/__tests__/push.test.ts
  </files>
  <read_first>
    artifacts/web/src/lib/auth.ts
    artifacts/web/src/lib/notifications.ts
    artifacts/web/src/lib/push.ts
    artifacts/web/src/lib/supabase/server.ts
    artifacts/web/src/lib/db.ts
    artifacts/web/package.json
    artifacts/web/tsconfig.json
  </read_first>
  <acceptance_criteria>
    - Vitest config resolves @/ alias to ./src/* matching tsconfig paths
    - Auth unit tests cover: getCurrentUser returns user/null, requireAuth redirects when no user, requireAuth returns user when authenticated, requireAdmin redirects when no user, requireAdmin redirects when user is not staff, requireAdmin returns user+staff when valid
    - Notification unit tests cover: notifyBillOverdue inserts notification + calls push + calls email when opted in, notifyBillOverdue skips email when opted out, notifyTicketUpdate same pattern, notifyNewMessage sends push but no email
    - Push unit tests cover: sendPushToUser skips when VAPID not configured, sendPushToUser sends to all subscriptions, sendPushToUser deletes expired subscription (410 status)
    - All mocking uses vi.mock for @/lib/supabase/server, @/lib/db, @/lib/push, @/lib/email, next/navigation
    - `pnpm --filter @workspace/web test` runs and all tests pass
  </acceptance_criteria>
  <action>
    1. Install vitest and @vitejs/plugin-react as devDependencies in artifacts/web/package.json. Add "test" script: "vitest run" and "test:watch" script: "vitest".

    2. Create artifacts/web/vitest.config.ts:
       - Import { defineConfig } from 'vitest/config' and path
       - Set resolve.alias: { '@': path.resolve(__dirname, './src') }
       - Set test.environment: 'node' (no DOM needed for these tests)
       - Set test.globals: true for describe/it/expect without imports
       - Include pattern: 'src/**/__tests__/**/*.test.ts'

    3. Create artifacts/web/src/lib/__tests__/auth.test.ts:
       - Mock '@/lib/supabase/server' createClient to return { auth: { getUser } } with controllable return values
       - Mock '@/lib/db' as a mock object with select().from().where() chain that returns controllable rows
       - Mock 'next/navigation' redirect to throw (simulating Next.js behavior)
       - Mock drizzle-orm eq function
       - Test getCurrentUser: returns { user: mockUser } when authenticated, returns { user: null } when not
       - Test requireAuth: returns user when authenticated, throws redirect('/login') when not
       - Test requireAdmin: returns { user, staff } when user is staff, throws redirect('/admin/login') when no user, throws redirect('/admin/login') when user is not staff (empty staffRows)
       - Use beforeEach to reset mocks

    4. Create artifacts/web/src/lib/__tests__/notifications.test.ts:
       - Mock '@/lib/push' sendPushToUser
       - Mock '@/lib/email' sendBillOverdueEmail, sendTicketUpdateEmail
       - Mock '@/lib/db' with insert().values() chain and select().from().where() chain
       - Test notifyBillOverdue: calls db.insert with correct notification data, calls sendPushToUser with title/body/url, calls email when user.emailNotifications is true, skips email when false
       - Test notifyTicketUpdate: similar pattern with ticket-specific data
       - Test notifyNewMessage: creates notification, sends push, does NOT send email
       - Verify fire-and-forget: errors in push/email don't throw (functions catch internally)

    5. Create artifacts/web/src/lib/__tests__/push.test.ts:
       - Mock 'web-push' module
       - Mock '@/lib/db' for subscription queries and deletion
       - Test sendPushToUser: when VAPID env vars missing, logs warning and returns without sending
       - Test sendPushToUser: sends notification to each subscription in DB
       - Test sendPushToUser: when webpush.sendNotification throws with statusCode 410, deletes that subscription from DB
       - Test sendPushToUser: when webpush.sendNotification throws non-410, logs error but continues
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/scla && pnpm --filter @workspace/web test 2>&1 | tail -30</automated>
  </verify>
  <done>Vitest configured, 15+ unit tests pass for auth helpers (6 tests), notification helpers (6+ tests), and push helpers (4 tests)</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for all API route handlers</name>
  <files>
    artifacts/web/src/app/api/tickets/[id]/messages/__tests__/route.test.ts
    artifacts/web/src/app/api/notifications/unread-count/__tests__/route.test.ts
    artifacts/web/src/app/api/cron/bill-overdue-check/__tests__/route.test.ts
    artifacts/web/src/app/api/push/subscribe/__tests__/route.test.ts
  </files>
  <read_first>
    artifacts/web/src/app/api/tickets/[id]/messages/route.ts
    artifacts/web/src/app/api/notifications/unread-count/route.ts
    artifacts/web/src/app/api/cron/bill-overdue-check/route.ts
    artifacts/web/src/app/api/push/subscribe/route.ts
    artifacts/web/src/lib/__tests__/auth.test.ts
  </read_first>
  <acceptance_criteria>
    - Each API route test file imports the GET/POST handler directly and calls it with mocked NextRequest
    - Tests cover auth (401 when unauthenticated), validation (400 for bad input), success (200/201), and edge cases
    - Ticket messages: GET returns messages list, POST creates message with resident/staff sender detection, POST rejects empty content, POST rejects invalid UUID
    - Unread count: GET returns { count } for authenticated user, GET returns 401 for unauthenticated
    - Cron bill-overdue: POST returns 401 without correct CRON_SECRET, POST processes overdue invoices and returns { processed, errors, total }
    - Push subscribe: POST stores subscription for authenticated user, POST returns 401 for unauthenticated
    - All tests use the same mocking patterns from Task 1 (vi.mock for supabase, db, etc.)
    - All tests pass via `pnpm --filter @workspace/web test`
  </acceptance_criteria>
  <action>
    1. Create artifacts/web/src/app/api/tickets/[id]/messages/__tests__/route.test.ts:
       - Import { GET, POST } from '../route'
       - Mock '@/lib/supabase/server', '@/lib/db', '@/lib/notifications', drizzle-orm operators
       - Create helper: mockNextRequest(url, options?) that returns a NextRequest-like object
       - Create helper: mockParams(id) that returns Promise resolving to { id }
       - Test GET: returns 401 when unauthenticated (mock getUser returns null)
       - Test GET: returns 400 for invalid UUID param
       - Test GET: returns 404 when ticket not found (mock db returns empty)
       - Test GET: returns 200 with messages array when ticket exists
       - Test POST: returns 401 when unauthenticated
       - Test POST: returns 400 when content is empty/missing
       - Test POST: returns 201 with message when resident sends (mock staffRows empty, userRows has user)
       - Test POST: returns 201 with staff senderType when staff sends (mock staffRows has row)

    2. Create artifacts/web/src/app/api/notifications/unread-count/__tests__/route.test.ts:
       - Import { GET } from '../route'
       - Mock supabase, db, and '@/lib/queries/notifications' getUnreadCount
       - Test GET: returns 401 when unauthenticated
       - Test GET: returns { count: 0 } when user not found in app DB
       - Test GET: returns { count: 5 } when getUnreadCount returns 5

    3. Create artifacts/web/src/app/api/cron/bill-overdue-check/__tests__/route.test.ts:
       - Import { POST } from '../route'
       - Mock '@/lib/db' and '@/lib/notifications'
       - Set process.env.CRON_SECRET = 'test-secret' in beforeEach
       - Test POST: returns 401 when x-cron-secret header is wrong
       - Test POST: returns 200 with { processed: 0, errors: 0, total: 0 } when no overdue invoices
       - Test POST: returns 200 with { processed: 2, errors: 0, total: 2 } when 2 overdue invoices found, and notifyBillOverdue called twice
       - Test POST: force=true changes the SQL condition (verify different query)

    4. Create artifacts/web/src/app/api/push/subscribe/__tests__/route.test.ts:
       - Read the actual route.ts first to understand the exact API contract
       - Import { POST } from '../route'
       - Mock supabase, db
       - Test POST: returns 401 when unauthenticated
       - Test POST: returns 200/201 when authenticated with valid subscription body
       - Test POST: returns 400 when body is missing/invalid
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/scla && pnpm --filter @workspace/web test 2>&1 | tail -30</automated>
  </verify>
  <done>15+ integration tests pass covering all 4 API routes with auth, validation, and success paths</done>
</task>

</tasks>

<verification>
- `cd artifacts/web && npx vitest run --reporter=verbose` shows all tests passing
- Test count is 30+ total (unit + integration)
- No test touches a real database or external service (all mocked)
</verification>

<success_criteria>
1. Vitest config exists and resolves @/ path alias correctly
2. Unit tests cover auth middleware (getCurrentUser, requireAuth, requireAdmin) — 6+ tests
3. Unit tests cover notification helpers (notifyBillOverdue, notifyTicketUpdate, notifyNewMessage) — 6+ tests
4. Unit tests cover push helper (sendPushToUser) — 4+ tests
5. Integration tests cover all 4 API route handlers — 15+ tests
6. All tests pass with `pnpm --filter @workspace/web test`
</success_criteria>

<output>
After completion, create `.planning/phases/31-testing-ci/31-01-SUMMARY.md`
</output>
