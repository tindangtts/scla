---
phase: 30-i18n-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/web/package.json
  - artifacts/web/next.config.ts
  - artifacts/web/src/app/layout.tsx
  - artifacts/web/src/app/(resident)/layout.tsx
  - artifacts/web/src/app/(admin)/layout.tsx
  - artifacts/web/src/i18n/request.ts
  - artifacts/web/src/i18n/routing.ts
  - artifacts/web/messages/en.json
  - artifacts/web/messages/my.json
  - artifacts/web/src/components/theme-provider.tsx
  - artifacts/web/src/components/theme-toggle.tsx
  - artifacts/web/src/components/locale-switcher.tsx
  - artifacts/web/src/middleware.ts
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03

must_haves:
  truths:
    - "Switching language between English and Myanmar changes all UI text without page reload"
    - "Dark mode toggle switches theme and persists across page refresh"
    - "System color-scheme preference is detected and applied on first visit"
    - "Bottom navigation bar is visible on mobile and hidden on desktop (lg breakpoint)"
  artifacts:
    - path: "artifacts/web/messages/en.json"
      provides: "English translation strings"
      contains: "nav.home"
    - path: "artifacts/web/messages/my.json"
      provides: "Myanmar translation strings"
      contains: "nav.home"
    - path: "artifacts/web/src/components/theme-provider.tsx"
      provides: "next-themes ThemeProvider wrapper"
      exports: ["ThemeProvider"]
    - path: "artifacts/web/src/components/theme-toggle.tsx"
      provides: "Dark/light mode toggle button"
      exports: ["ThemeToggle"]
    - path: "artifacts/web/src/components/locale-switcher.tsx"
      provides: "Language switcher component"
      exports: ["LocaleSwitcher"]
  key_links:
    - from: "artifacts/web/src/app/layout.tsx"
      to: "next-themes"
      via: "ThemeProvider wrapping children"
      pattern: "ThemeProvider"
    - from: "artifacts/web/src/app/(resident)/layout.tsx"
      to: "messages/*.json"
      via: "useTranslations hook for nav labels"
      pattern: "useTranslations"
    - from: "artifacts/web/src/app/(admin)/layout.tsx"
      to: "messages/*.json"
      via: "useTranslations hook for sidebar labels"
      pattern: "useTranslations"
---

<objective>
Add multi-language support (English + Myanmar), dark mode with system detection and manual toggle, and responsive bottom navigation to the SCLA app.

Purpose: Myanmar residents need native language support; dark mode improves usability in low-light; bottom nav must hide on desktop for proper layout.
Output: i18n provider with translation files, theme provider with toggle, responsive bottom nav.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-i18n-ux-polish/30-CONTEXT.md

@artifacts/web/src/app/layout.tsx
@artifacts/web/src/app/(resident)/layout.tsx
@artifacts/web/src/app/(admin)/layout.tsx
@artifacts/web/src/app/globals.css
@artifacts/web/next.config.ts
@artifacts/web/package.json
@artifacts/web/src/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create i18n + theme infrastructure</name>
  <files>
    artifacts/web/package.json,
    artifacts/web/next.config.ts,
    artifacts/web/src/i18n/request.ts,
    artifacts/web/src/i18n/routing.ts,
    artifacts/web/messages/en.json,
    artifacts/web/messages/my.json,
    artifacts/web/src/components/theme-provider.tsx,
    artifacts/web/src/components/theme-toggle.tsx,
    artifacts/web/src/components/locale-switcher.tsx,
    artifacts/web/src/app/layout.tsx,
    artifacts/web/src/middleware.ts
  </files>
  <action>
1. Install dependencies from the web workspace root:
   - `next-intl` for i18n (App Router compatible, supports Server Components)
   - `next-themes` for dark mode (lightweight, handles system detection)
   Run: `cd artifacts/web && pnpm add next-intl next-themes`

2. Create `artifacts/web/src/i18n/request.ts`:
   - Use `getRequestConfig` from `next-intl/server`
   - Load messages from `../../messages/${locale}.json`
   - Default locale: "en", supported locales: ["en", "my"]
   - Use cookie-based locale detection (cookie name: "NEXT_LOCALE") so locale changes don't require URL path changes

3. Create `artifacts/web/src/i18n/routing.ts`:
   - Export `locales = ["en", "my"] as const`
   - Export `defaultLocale = "en"`
   - Export type `Locale = (typeof locales)[number]`

4. Create `artifacts/web/messages/en.json` with translation keys organized by namespace:
   ```json
   {
     "nav": { "home": "Home", "bills": "Bills", "starAssist": "Star Assist", "bookings": "Bookings", "more": "More" },
     "header": { "appName": "Star City Living", "logout": "Logout" },
     "home": { "welcome": "Welcome", "quickActions": "Quick Actions", "recentBills": "Recent Bills", "activeTickets": "Active Tickets" },
     "bills": { "title": "Bills", "paid": "Paid", "unpaid": "Unpaid", "overdue": "Overdue", "payNow": "Pay Now", "amount": "Amount", "dueDate": "Due Date" },
     "wallet": { "title": "Wallet", "balance": "Balance", "transactions": "Transactions", "topUp": "Top Up" },
     "tickets": { "title": "Star Assist", "new": "New Ticket", "category": "Category", "status": "Status", "open": "Open", "inProgress": "In Progress", "resolved": "Resolved", "closed": "Closed" },
     "bookings": { "title": "Bookings", "facilities": "Facilities", "myBookings": "My Bookings", "book": "Book", "cancel": "Cancel", "date": "Date", "time": "Time" },
     "discover": { "title": "Discover", "announcements": "Announcements", "promotions": "Promotions", "newsletters": "Newsletters" },
     "info": { "title": "Info Centre", "search": "Search articles..." },
     "profile": { "title": "Profile", "edit": "Edit Profile", "name": "Name", "email": "Email", "phone": "Phone", "unit": "Unit" },
     "notifications": { "title": "Notifications", "preferences": "Preferences", "markRead": "Mark as read", "noNotifications": "No notifications" },
     "auth": { "login": "Login", "register": "Register", "email": "Email", "password": "Password", "forgotPassword": "Forgot Password?", "noAccount": "Don't have an account?", "hasAccount": "Already have an account?" },
     "common": { "loading": "Loading...", "error": "Something went wrong", "retry": "Retry", "save": "Save", "cancel": "Cancel", "delete": "Delete", "edit": "Edit", "back": "Back", "submit": "Submit", "search": "Search", "noResults": "No results found", "viewAll": "View All" },
     "settings": { "language": "Language", "theme": "Theme", "dark": "Dark", "light": "Light", "system": "System" },
     "admin": { "dashboard": "Dashboard", "users": "Users", "upgradeRequests": "Upgrade Requests", "tickets": "Tickets", "facilities": "Facilities", "content": "Content", "staff": "Staff", "auditLogs": "Audit Logs", "wallets": "Wallets", "signOut": "Sign Out" }
   }
   ```

5. Create `artifacts/web/messages/my.json` with Myanmar translations for all the same keys:
   Use proper Myanmar/Burmese Unicode text. Key translations:
   - Home = "ပင်မ", Bills = "ဘေလ်များ", Star Assist = "စတား အကူအညီ", Bookings = "ကြိုတင်မှာကြားခြင်း", More = "နောက်ထပ်"
   - Login = "ဝင်ရောက်ရန်", Register = "စာရင်းသွင်းရန်", Logout = "ထွက်ရန်"
   - Loading = "ခဏစောင့်ပါ...", Error = "တစ်ခုခုမှားနေပါသည်", Save = "သိမ်းမည်", Cancel = "ပယ်ဖျက်မည်"
   - Dashboard = "ဒက်ရှ်ဘုတ်", Users = "အသုံးပြုသူများ", Notifications = "အသိပေးချက်များ"
   - Profile = "ပရိုဖိုင်", Wallet = "ပိုက်ဆံအိတ်", Facilities = "အဆောက်အအုံများ"
   All keys must match en.json structure exactly.

6. Create `artifacts/web/src/components/theme-provider.tsx`:
   - Client component ("use client")
   - Import ThemeProvider from next-themes
   - Wrap children with ThemeProvider, props: attribute="class", defaultTheme="system", enableSystem=true, disableTransitionOnChange=true
   - Export as named export `ThemeProvider`

7. Create `artifacts/web/src/components/theme-toggle.tsx`:
   - Client component ("use client")
   - Import useTheme from next-themes
   - Render a button that cycles: system -> light -> dark -> system
   - Show Sun icon for light, Moon icon for dark, Monitor icon for system (use lucide-react icons)
   - Add aria-label for accessibility
   - Style: `p-2 rounded-md hover:bg-accent` with dark mode support

8. Create `artifacts/web/src/components/locale-switcher.tsx`:
   - Client component ("use client")
   - Read current locale from cookie "NEXT_LOCALE" (or default "en")
   - On click, set document.cookie for "NEXT_LOCALE" to the other locale, then call `router.refresh()` to reload translations without full page reload
   - Display "EN" or "MY" as the current language indicator
   - Style: compact button matching theme-toggle style

9. Update `artifacts/web/src/app/layout.tsx`:
   - Import NextIntlClientProvider from next-intl
   - Import getMessages, getLocale from next-intl/server
   - Import ThemeProvider from @/components/theme-provider
   - In the async function body, call `const locale = await getLocale()` and `const messages = await getMessages()`
   - Set `<html lang={locale} suppressHydrationWarning>` (suppressHydrationWarning needed for next-themes)
   - Wrap body children: `<ThemeProvider><NextIntlClientProvider messages={messages}>{children}</NextIntlClientProvider></ThemeProvider>`

10. Update `artifacts/web/next.config.ts`:
    - Add `const withNextIntl = require("next-intl/plugin")("./src/i18n/request.ts")` (use createNextIntlPlugin for ESM)
    - Wrap the config: `export default withNextIntl(nextConfig)`
    - Actually use: `import createNextIntlPlugin from "next-intl/plugin"; const withNextIntl = createNextIntlPlugin("./src/i18n/request.ts");`

11. Update `artifacts/web/src/middleware.ts`:
    - The existing middleware handles auth. Do NOT use next-intl middleware routing (we use cookie-based locale, not path-based).
    - No changes needed to middleware if cookie-based approach is used. Only add locale cookie reading in i18n/request.ts using `cookies()` from next/headers.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/scla && pnpm --filter @workspace/web exec next build 2>&1 | tail -20</automated>
  </verify>
  <done>next-intl and next-themes installed, translation files exist with all keys, ThemeProvider wraps app, locale switcher and theme toggle components created, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Wire i18n keys into layouts, add dark mode support, fix responsive bottom nav</name>
  <files>
    artifacts/web/src/app/(resident)/layout.tsx,
    artifacts/web/src/app/(admin)/layout.tsx,
    artifacts/web/src/app/login/page.tsx,
    artifacts/web/src/app/register/page.tsx,
    artifacts/web/src/app/admin/login/page.tsx,
    artifacts/web/src/app/(resident)/more/page.tsx
  </files>
  <action>
1. Update `artifacts/web/src/app/(resident)/layout.tsx`:
   - The layout is a Server Component (uses `await requireAuth()`). For translations, use `getTranslations` from `next-intl/server` (NOT useTranslations which is client-only).
   - Call `const t = await getTranslations("nav")` and `const th = await getTranslations("header")` at the top of the function.
   - Replace hardcoded header text: "Star City Living" -> `{th("appName")}`, "Logout" -> `{th("logout")}`
   - Replace hardcoded nav labels: "Home" -> `{t("home")}`, "Bills" -> `{t("bills")}`, "Star Assist" -> `{t("starAssist")}`, "Bookings" -> `{t("bookings")}`, "More" -> `{t("more")}`
   - Add ThemeToggle and LocaleSwitcher to the header (import from @/components), place them in the flex items-center gap between display name and logout button
   - Fix bottom nav responsive: add `lg:hidden` to the bottom nav element so it hides on desktop
   - Add `pb-16 lg:pb-0` to main element (remove bottom padding on desktop where nav is hidden)
   - Dark mode the header: change `bg-blue-600` to `bg-blue-600 dark:bg-blue-900`, `bg-blue-700 hover:bg-blue-800` to `bg-blue-700 hover:bg-blue-800 dark:bg-blue-800 dark:hover:bg-blue-700`
   - Dark mode the bottom nav: change `bg-white border-t` to `bg-white dark:bg-gray-900 border-t dark:border-gray-700`, `text-gray-600` to `text-gray-600 dark:text-gray-300`

2. Update `artifacts/web/src/app/(admin)/layout.tsx`:
   - Server Component — use `getTranslations` from `next-intl/server`.
   - Call `const t = await getTranslations("admin")` at top.
   - Replace navItems labels with translation keys: "Dashboard" -> t("dashboard"), "Users" -> t("users"), etc.
   - Since navItems is a static array, restructure: create array with key names, map using t(key) for labels.
   - Dark mode the sidebar: `bg-gray-900` stays (already dark), the main area `bg-gray-50` -> `bg-gray-50 dark:bg-gray-950`
   - Add ThemeToggle and LocaleSwitcher to the sidebar, below the staff info block.

3. Update `artifacts/web/src/app/login/page.tsx`:
   - These are Server Components if they use server actions. Use `getTranslations` from `next-intl/server`.
   - Add translations for login form labels: email, password, login button, register link.
   - If the page is a Client Component (has "use client"), use `useTranslations` from `next-intl` instead.
   - Read the file first to determine component type, then apply the correct approach.

4. Update `artifacts/web/src/app/register/page.tsx`:
   - Same approach as login — translate form labels and buttons.

5. Update `artifacts/web/src/app/admin/login/page.tsx`:
   - Same approach — translate admin login form.

6. Update `artifacts/web/src/app/(resident)/more/page.tsx`:
   - Add a "Language" section with the LocaleSwitcher component.
   - Add a "Theme" section with the ThemeToggle component (or link to settings).
   - Translate the page title and section labels using getTranslations.

Note: Do NOT attempt to translate every single page in this task. The infrastructure (providers, translation files, hooks) is in place. Remaining pages can consume translations incrementally. The priority pages are: layouts (nav), auth pages, and the More/settings page.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/scla && pnpm --filter @workspace/web exec next build 2>&1 | tail -20</automated>
  </verify>
  <done>Resident layout nav shows translated labels (en/my switchable), admin sidebar shows translated labels, dark mode toggle works in both layouts, bottom nav hidden on lg+ screens, login/register pages have translated labels, More page has language/theme settings</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @workspace/web build` succeeds without errors
2. Start dev server, visit resident layout — bottom nav visible on mobile, hidden on desktop
3. Click locale switcher — all nav labels change to Myanmar without page reload
4. Click theme toggle — dark mode applies, background/text colors change across all visible elements
5. Refresh page — theme and locale preferences persist (cookie/localStorage)
</verification>

<success_criteria>
- Switching locale between en/my updates nav labels, header text, and auth form labels without full page reload
- Dark mode toggle cycles through system/light/dark and persists across refresh
- Bottom nav shows on mobile viewports, hides on lg+ breakpoint
- Build succeeds with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/30-i18n-ux-polish/30-01-SUMMARY.md`
</output>
