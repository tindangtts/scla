---
phase: 16-i18n-completion-and-auth-middleware-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/scla/src/pages/profile.tsx
  - artifacts/scla/src/pages/ticket-detail.tsx
  - artifacts/scla/src/pages/wallet.tsx
  - artifacts/scla/src/pages/upgrade.tsx
  - artifacts/scla/src/pages/bill-detail.tsx
  - artifacts/scla/src/pages/discover-detail.tsx
  - artifacts/scla/src/pages/info.tsx
  - artifacts/scla/src/pages/info-article.tsx
autonomous: true
requirements:
  - ENH-01

must_haves:
  truths:
    - "All 8 remaining resident-facing pages call useTranslation() and use t() for user-visible strings"
    - "Language switching in profile immediately updates all 8 pages with no hardcoded English strings visible"
    - "Translation keys used in these pages exist in en.json and my.json"
  artifacts:
    - path: "artifacts/scla/src/pages/profile.tsx"
      provides: "Profile page translated"
      contains: "useTranslation"
    - path: "artifacts/scla/src/pages/ticket-detail.tsx"
      provides: "Ticket detail page translated"
      contains: "useTranslation"
    - path: "artifacts/scla/src/pages/wallet.tsx"
      provides: "Wallet page translated"
      contains: "useTranslation"
    - path: "artifacts/scla/src/pages/upgrade.tsx"
      provides: "Upgrade page translated"
      contains: "useTranslation"
    - path: "artifacts/scla/src/pages/bill-detail.tsx"
      provides: "Bill detail page translated"
      contains: "useTranslation"
    - path: "artifacts/scla/src/pages/discover-detail.tsx"
      provides: "Discover detail page translated"
      contains: "useTranslation"
    - path: "artifacts/scla/src/pages/info.tsx"
      provides: "Info page translated"
      contains: "useTranslation"
    - path: "artifacts/scla/src/pages/info-article.tsx"
      provides: "Info article page translated"
      contains: "useTranslation"
  key_links:
    - from: "each page component"
      to: "react-i18next"
      via: "import { useTranslation } from 'react-i18next'"
      pattern: "useTranslation"
    - from: "t() calls"
      to: "artifacts/scla/src/locales/en.json"
      via: "dot-notation keys"
      pattern: "t\\(\"(profile|wallet|upgrade|ticket|discover|info)\\."
---

<objective>
Wire useTranslation on the 8 resident-facing pages that were missed in Phase 14-06.

Purpose: ENH-01 gap closure — every resident-facing page must respond to language switching. Translation keys for all 8 pages already exist in en.json/my.json; this plan is purely wiring, not key creation.
Output: 8 page files updated to use useTranslation() and t() for all user-visible strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Translation keys available for these pages — extracted from artifacts/scla/src/locales/en.json -->
<!-- Use ONLY these keys. Do NOT invent new keys. -->

Profile page keys:
  profile.title, profile.signIn, profile.signInBtn, profile.createAccount,
  profile.unitDetails, profile.development, profile.unitNumber, profile.residentId,
  profile.notifications, profile.wallet, profile.contactInfo, profile.email,
  profile.phone, profile.signOut, profile.language, profile.theme, profile.settings,
  profile.langEnglish, profile.langMyanmar, profile.themeDark, profile.themeLight,
  profile.themeSystem

Wallet page keys:
  wallet.title

Upgrade page keys:
  upgrade.title

Discover detail page keys:
  discover.title  (use common.back for back button)

Info page keys:
  info.title

Common keys (use across all pages where appropriate):
  common.loading, common.retry, common.back, common.cancel, common.save, common.close

Ticket detail keys (use existing ticket.* keys already in en.json):
  tickets.title (fallback — ticket-detail hardcodes category labels)

Bill detail keys:
  bills.title (fallback — bill-detail has dynamic invoice fields)

<!-- Established pattern from Phase 14-06 (home.tsx, bills.tsx, etc.): -->
<!-- 1. Add: import { useTranslation } from "react-i18next"; -->
<!-- 2. Inside component body: const { t } = useTranslation(); -->
<!-- 3. Replace hardcoded user-visible strings with t("key") calls -->
<!-- 4. STATUS_FILTERS arrays that use translated strings must move inside component scope -->
<!--    so t() is accessible — see Phase 14 decision in STATE.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire useTranslation on profile, wallet, upgrade, info</name>
  <files>
    artifacts/scla/src/pages/profile.tsx,
    artifacts/scla/src/pages/wallet.tsx,
    artifacts/scla/src/pages/upgrade.tsx,
    artifacts/scla/src/pages/info.tsx
  </files>
  <read_first>
    artifacts/scla/src/pages/profile.tsx
    artifacts/scla/src/pages/wallet.tsx
    artifacts/scla/src/pages/upgrade.tsx
    artifacts/scla/src/pages/info.tsx
    artifacts/scla/src/pages/home.tsx (lines 1-20 for import/hook pattern reference)
  </read_first>
  <action>
    For each of the 4 files, apply the Phase 14 useTranslation pattern:

    1. Add `import { useTranslation } from "react-i18next";` alongside existing imports.
    2. Inside the component function body (first line after destructuring hooks), add `const { t } = useTranslation();`.
    3. Replace every hardcoded user-visible string with a t() call using the keys listed in the interfaces block above.

    Specific changes required per file:

    **profile.tsx** — Already imports useLanguage/useTheme; this page has the most keys to wire.
    Replace:
    - "Sign in to view your profile" → t("profile.signIn")
    - "Sign In to Account" → t("profile.signInBtn")
    - "Create a guest account" → t("profile.createAccount")
    - "Profile" heading → t("profile.title")
    - "Unit Details" section heading → t("profile.unitDetails")
    - "Development" label → t("profile.development")
    - "Unit Number" label → t("profile.unitNumber")
    - "Resident ID" label → t("profile.residentId")
    - "Notifications" menu item → t("profile.notifications")
    - "Wallet & Deposits" menu item → t("profile.wallet")
    - "Contact Info" section → t("profile.contactInfo")
    - "Email Address" label → t("profile.email")
    - "Phone Number" label → t("profile.phone")
    - "Sign Out of Account" → t("profile.signOut")
    - "Language" section → t("profile.language")
    - "Theme" section → t("profile.theme")
    - "App Settings" section → t("profile.settings")
    - "English" option → t("profile.langEnglish")
    - "မြန်မာ (Myanmar)" option → t("profile.langMyanmar")
    - "Dark Mode" option → t("profile.themeDark")
    - "Light Mode" option → t("profile.themeLight")
    - "System Default" option → t("profile.themeSystem")

    **wallet.tsx** — Replace the "Wallet & Deposits" page title with t("wallet.title").
    Replace common.loading for loading states if any hardcoded "Loading..." exists.

    **upgrade.tsx** — Replace the "Resident Upgrade" page title with t("upgrade.title").
    Replace common.loading and common.back for any hardcoded equivalents.

    **info.tsx** — Replace the "Info Centre" page title with t("info.title").
    Replace common.loading and common.back for any hardcoded equivalents.

    CRITICAL: Do NOT create new keys in en.json/my.json. Only use keys that already exist (listed in interfaces block). For any string that has no matching key, leave it hardcoded — do not invent keys.
  </action>
  <verify>
    <automated>grep -l "useTranslation" artifacts/scla/src/pages/profile.tsx artifacts/scla/src/pages/wallet.tsx artifacts/scla/src/pages/upgrade.tsx artifacts/scla/src/pages/info.tsx | wc -l | grep -q "^4$" && echo "PASS: all 4 files have useTranslation" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" artifacts/scla/src/pages/profile.tsx` returns >= 1
    - `grep -c "useTranslation" artifacts/scla/src/pages/wallet.tsx` returns >= 1
    - `grep -c "useTranslation" artifacts/scla/src/pages/upgrade.tsx` returns >= 1
    - `grep -c "useTranslation" artifacts/scla/src/pages/info.tsx` returns >= 1
    - `grep -c 'profile\.title\|profile\.signIn\|profile\.signOut' artifacts/scla/src/pages/profile.tsx` returns >= 3
  </acceptance_criteria>
  <done>All 4 pages import useTranslation and call t() for their primary user-visible strings. No new en.json keys created.</done>
</task>

<task type="auto">
  <name>Task 2: Wire useTranslation on ticket-detail, bill-detail, discover-detail, info-article</name>
  <files>
    artifacts/scla/src/pages/ticket-detail.tsx,
    artifacts/scla/src/pages/bill-detail.tsx,
    artifacts/scla/src/pages/discover-detail.tsx,
    artifacts/scla/src/pages/info-article.tsx
  </files>
  <read_first>
    artifacts/scla/src/pages/ticket-detail.tsx
    artifacts/scla/src/pages/bill-detail.tsx
    artifacts/scla/src/pages/discover-detail.tsx
    artifacts/scla/src/pages/info-article.tsx
  </read_first>
  <action>
    Apply the same Phase 14 useTranslation pattern to the 4 detail pages.

    For each file:
    1. Add `import { useTranslation } from "react-i18next";`
    2. Add `const { t } = useTranslation();` at the top of the component body.
    3. Replace user-visible hardcoded strings with t() calls for keys that exist.

    **ticket-detail.tsx** — Use common.back for any back button text. Use common.loading for skeleton/loading states. The categoryLabels map is a data lookup (not user preference), leave it hardcoded unless a matching key exists.

    **bill-detail.tsx** — Use common.back for back button. Use common.loading for loading states. Use bills.pay for "Pay Now" if present.

    **discover-detail.tsx** — Use discover.title for page title. Use common.back for back button. Use common.loading for loading states.

    **info-article.tsx** — Use info.title for page/section title. Use common.back for back button. Use common.loading for loading states.

    CRITICAL: Do NOT create new keys in en.json/my.json. Only use existing keys from the interfaces block. Dynamic content (announcement title, article body, invoice amounts) is data, not translated strings — leave it as-is.
  </action>
  <verify>
    <automated>grep -l "useTranslation" artifacts/scla/src/pages/ticket-detail.tsx artifacts/scla/src/pages/bill-detail.tsx artifacts/scla/src/pages/discover-detail.tsx artifacts/scla/src/pages/info-article.tsx | wc -l | grep -q "^4$" && echo "PASS: all 4 files have useTranslation" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" artifacts/scla/src/pages/ticket-detail.tsx` returns >= 1
    - `grep -c "useTranslation" artifacts/scla/src/pages/bill-detail.tsx` returns >= 1
    - `grep -c "useTranslation" artifacts/scla/src/pages/discover-detail.tsx` returns >= 1
    - `grep -c "useTranslation" artifacts/scla/src/pages/info-article.tsx` returns >= 1
    - `grep -rn "useTranslation" artifacts/scla/src/pages/ | wc -l` returns >= 18 (10 existing + 8 new)
  </acceptance_criteria>
  <done>All 4 detail pages import useTranslation and call t() for navigational/UI strings. Dynamic data content is correctly left untranslated.</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -rl "useTranslation" artifacts/scla/src/pages/ | wc -l` must return 18 (10 previously wired + 8 new)
- `grep -rn "import { useTranslation }" artifacts/scla/src/pages/profile.tsx artifacts/scla/src/pages/ticket-detail.tsx artifacts/scla/src/pages/wallet.tsx artifacts/scla/src/pages/upgrade.tsx artifacts/scla/src/pages/bill-detail.tsx artifacts/scla/src/pages/discover-detail.tsx artifacts/scla/src/pages/info.tsx artifacts/scla/src/pages/info-article.tsx | wc -l` must return 8
- en.json line count must not change (no new keys added): `wc -l artifacts/scla/src/locales/en.json` returns 109
</verification>

<success_criteria>
ENH-01 gap closed: All resident-facing pages use useTranslation. Language switching from the profile page updates UI text on all 8 newly-wired pages. No hardcoded English UI strings remain for strings that have a key in en.json.
</success_criteria>

<output>
After completion, create `.planning/phases/16-i18n-completion-and-auth-middleware-cleanup/16-01-SUMMARY.md`
</output>
