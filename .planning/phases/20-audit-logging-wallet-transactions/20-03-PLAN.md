---
phase: 20-audit-logging-wallet-transactions
plan: 03
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified:
  - artifacts/admin/src/pages/audit-logs.tsx
  - artifacts/admin/src/components/layout/admin-layout.tsx
  - artifacts/admin/src/App.tsx
  - artifacts/scla/src/pages/wallet.tsx
autonomous: false
requirements: [AUDIT-02, WALLET-01, WALLET-04]

must_haves:
  truths:
    - "Admin can view an audit log list showing who did what and when"
    - "Admin can filter audit logs by date range, actor, and action type"
    - "Resident wallet page shows real transaction data from API with type filter"
    - "Wallet page has tab/filter for Security Deposit vs Wallet transactions"
  artifacts:
    - path: "artifacts/admin/src/pages/audit-logs.tsx"
      provides: "Admin audit log list page with filters"
      min_lines: 80
    - path: "artifacts/admin/src/components/layout/admin-layout.tsx"
      provides: "Updated admin sidebar with Audit Logs nav item"
    - path: "artifacts/admin/src/App.tsx"
      provides: "Route for /audit-logs page"
    - path: "artifacts/scla/src/pages/wallet.tsx"
      provides: "Updated wallet page with type filters and real API data"
  key_links:
    - from: "artifacts/admin/src/pages/audit-logs.tsx"
      to: "/api/admin/audit-logs"
      via: "fetch with query params"
      pattern: "admin/audit-logs"
    - from: "artifacts/scla/src/pages/wallet.tsx"
      to: "/api/wallet"
      via: "useGetWallet hook or direct fetch"
      pattern: "wallet"
---

<objective>
Build the admin audit log viewer page and update the resident wallet page to show real transaction data with filtering.

Purpose: Admins get visibility into who performed which actions and when. Residents see their actual wallet and deposit transactions with type filters.
Output: New admin audit logs page, updated admin sidebar/routes, updated resident wallet page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-audit-logging-wallet-transactions/20-CONTEXT.md
@.planning/phases/20-audit-logging-wallet-transactions/20-01-SUMMARY.md
@.planning/phases/20-audit-logging-wallet-transactions/20-02-SUMMARY.md

@artifacts/admin/src/App.tsx
@artifacts/admin/src/components/layout/admin-layout.tsx
@artifacts/admin/src/pages/staff.tsx
@artifacts/scla/src/pages/wallet.tsx

<interfaces>
<!-- API endpoints from Plans 01 and 02 -->

GET /api/admin/audit-logs?action=&actorId=&from=&to=&page=&limit=
Response: { logs: [{ id, actorId, actorEmail, action, targetType, targetId, metadata, createdAt }], total, page, limit }

GET /api/wallet?type=credit|debit&page=&limit=
Response: { balance: number, transactions: [{ id, type, amount, description, reference, reason, createdAt }], total, page, limit }

GET /api/deposit?type=credit|debit&page=&limit=
Response: { balance: number, transactions: [...], total, page, limit }

Admin audit action enum values: upgrade_approve, upgrade_reject, booking_cancel, staff_create, staff_deactivate, staff_update, content_create, content_update, content_delete, wallet_adjust
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin audit logs page and wire into layout</name>
  <files>artifacts/admin/src/pages/audit-logs.tsx, artifacts/admin/src/components/layout/admin-layout.tsx, artifacts/admin/src/App.tsx</files>
  <read_first>
    - artifacts/admin/src/pages/staff.tsx (pattern for admin page: AdminLayout, useAdminAuth, tanstack query, table layout)
    - artifacts/admin/src/components/layout/admin-layout.tsx (navItems array to extend)
    - artifacts/admin/src/App.tsx (route registration pattern)
  </read_first>
  <action>
**Create `artifacts/admin/src/pages/audit-logs.tsx`:**

Follow the pattern from staff.tsx for consistent admin page structure:

1. Import AdminLayout, useAdminAuth, useQuery from @tanstack/react-query, lucide icons (ScrollText, Search, Filter)
2. Define a `fetchAuditLogs` function that calls `GET /api/admin/audit-logs` with Bearer token and query params
3. State for filters: `action` (select dropdown), `from` date (input type="date"), `to` date (input type="date"), `page` (number)
4. Use `useQuery` with queryKey including all filter values
5. Layout:
   - AdminLayout wrapper
   - Page title "Audit Logs" with ScrollText icon
   - Filter bar: three inputs in a row — Action dropdown (all actions + "All"), From date, To date — with a "Search" button
   - Table with columns: Date/Time, Actor (email), Action (formatted — replace underscores with spaces, title case), Target Type, Target ID, Details (show metadata as truncated JSON or key info)
   - Pagination controls at bottom (Previous/Next buttons, page X of Y)
6. Format action values for display: "upgrade_approve" -> "Upgrade Approve", etc.
7. Format createdAt as locale date+time string
8. Show metadata as a small expandable section or tooltip — simplest: just show first 50 chars of JSON.stringify(metadata)

**Update `artifacts/admin/src/components/layout/admin-layout.tsx`:**

1. Import `ScrollText` from lucide-react (add to existing import)
2. Add to navItems array after "Staff": `{ label: "Audit Logs", icon: ScrollText, href: "/audit-logs" }`

**Update `artifacts/admin/src/App.tsx`:**

1. Add import: `import AuditLogsPage from "@/pages/audit-logs";`
2. Add route: `<Route path="/audit-logs" component={AuditLogsPage} />` (after /staff route)
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/scla && npx tsc --noEmit --project artifacts/admin/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "audit-logs" artifacts/admin/src/pages/audit-logs.tsx
    - grep -q "Audit Logs" artifacts/admin/src/components/layout/admin-layout.tsx
    - grep -q "ScrollText" artifacts/admin/src/components/layout/admin-layout.tsx
    - grep -q "AuditLogsPage" artifacts/admin/src/App.tsx
    - grep -q "audit-logs" artifacts/admin/src/App.tsx
    - grep -q "action" artifacts/admin/src/pages/audit-logs.tsx
    - grep -q "from.*to" artifacts/admin/src/pages/audit-logs.tsx
  </acceptance_criteria>
  <done>Admin audit logs page shows table with date/actor/action filters, accessible from sidebar nav, route registered in App.tsx</done>
</task>

<task type="auto">
  <name>Task 2: Update resident wallet page with real data and filters</name>
  <files>artifacts/scla/src/pages/wallet.tsx</files>
  <read_first>
    - artifacts/scla/src/pages/wallet.tsx (current wallet page — uses useGetWallet/useGetDeposit hooks)
    - artifacts/scla/src/pages/bills.tsx (pattern for filter tabs in resident app)
  </read_first>
  <action>
**Update `artifacts/scla/src/pages/wallet.tsx`:**

The page already uses `useGetWallet` and `useGetDeposit` hooks from @workspace/api-client-react. The API response shape is now `{ balance, transactions, total, page, limit }` (from Plan 02). The hooks should still work since the response top-level keys (balance, transactions) are the same.

Changes needed:

1. **Add a tab/filter toggle** at the top of the transaction sections: "Wallet" | "Security Deposit"
   - Add state: `const [activeTab, setActiveTab] = useState<"wallet" | "deposit">("wallet")`
   - Render two tab buttons above the transaction list (styled like pill buttons — one active with bg-primary, one muted)
   - When "Wallet" active, show wallet transactions. When "Security Deposit" active, show deposit transactions.
   - This replaces the current side-by-side layout of "Wallet Transactions" and "Deposit History" sections

2. **Add type filter** within each tab: "All" | "Credits" | "Debits"
   - Add state: `const [typeFilter, setTypeFilter] = useState<"all" | "credit" | "debit">("all")`
   - Three small filter chips below the tab bar
   - Filter transactions client-side: if typeFilter !== "all", filter `tx.type === typeFilter`

3. **Show reason field** on transactions that have one (admin adjustments, deposit deductions):
   - Below the description, if `tx.reason` exists, show it in italic muted text: `Reason: {tx.reason}`

4. **Keep existing styling patterns** — rounded-[1.5rem] cards, emerald for credits, red for debits, gradient header, etc.

5. **Handle the deposit tab transactions** — deposit debits should show with TrendingDown icon in red (currently all deposit transactions show as TrendingUp in blue). Check tx.type and render accordingly.

6. Reset typeFilter to "all" when switching tabs.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/scla && npx tsc --noEmit --project artifacts/scla/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "activeTab" artifacts/scla/src/pages/wallet.tsx
    - grep -q "Security Deposit" artifacts/scla/src/pages/wallet.tsx
    - grep -q "typeFilter" artifacts/scla/src/pages/wallet.tsx
    - grep -q "reason" artifacts/scla/src/pages/wallet.tsx
    - grep -q "credit.*debit" artifacts/scla/src/pages/wallet.tsx
  </acceptance_criteria>
  <done>Wallet page shows tab toggle between Wallet and Security Deposit, type filter (all/credit/debit), reason field displayed when present, deposit debits show correctly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify UI for audit logs and wallet pages</name>
  <files>artifacts/admin/src/pages/audit-logs.tsx, artifacts/scla/src/pages/wallet.tsx</files>
  <action>Human visual verification of both UI pages built in Tasks 1 and 2. No code changes needed — this is a review checkpoint.</action>
  <what-built>Admin audit log viewer page with date/actor/action filters, and updated resident wallet page with tab toggle (Wallet vs Security Deposit) and type filters (All/Credit/Debit).</what-built>
  <how-to-verify>
    1. Start the admin app and navigate to the sidebar — confirm "Audit Logs" appears in the nav
    2. Click "Audit Logs" — confirm the page loads with a table and filter controls (action dropdown, date from/to)
    3. If any audit data exists, confirm rows display with date, actor email, action, target type, target ID
    4. Start the resident app and navigate to the Wallet page
    5. Confirm tab toggle shows "Wallet" and "Security Deposit" tabs
    6. Confirm type filter chips (All, Credits, Debits) appear below tabs
    7. Switch between tabs — confirm transactions update accordingly
    8. Apply a type filter — confirm filtering works
    9. If any transaction has a reason, confirm it displays below the description
  </how-to-verify>
  <verify>
    <automated>echo "Human verification checkpoint — no automated check"</automated>
  </verify>
  <done>User confirms both admin audit log page and resident wallet page render correctly with all filters functional</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project artifacts/admin/tsconfig.json` passes
- `npx tsc --noEmit --project artifacts/scla/tsconfig.json` passes
- Admin audit logs page accessible from sidebar, shows filterable table
- Resident wallet page has tab toggle and type filter
</verification>

<success_criteria>
- Admin can view audit log list with date, actor email, action, target, and metadata columns
- Admin can filter audit logs by action type, date range
- Audit Logs appears in admin sidebar navigation
- Resident wallet page has Wallet / Security Deposit tab toggle
- Resident can filter transactions by type (credit/debit)
- Deposit deductions show with reason field
</success_criteria>

<output>
After completion, create `.planning/phases/20-audit-logging-wallet-transactions/20-03-SUMMARY.md`
</output>
